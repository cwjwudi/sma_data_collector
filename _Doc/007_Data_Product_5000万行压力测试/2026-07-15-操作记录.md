# Data_Product 5000 万行压力测试操作记录

## 只读盘点

- 时间：2026-07-15（Asia/Shanghai）
- 已读取 MariaDB 版本、源表建表语句、列定义、索引、33 行样本和数据库参数。
- 未对 `sma_data_test1` 执行任何写操作。
- MariaDB：10.11.11；InnoDB Buffer Pool：16 MiB；Redo Log：96 MiB；binlog 未启用。
- 当前所有数据库已分配数据与索引约 83.8 MiB；数据库接口无法确认宿主机剩余磁盘。

## 待执行及结果

- 目标库创建：已创建 `sma_data_stress_test`，仅含 `Data_Product`。
- 100,000 行试写：3.516 秒，约 28,441 行/秒。
- 1,000,000 行校准：累计续写约 31.1 秒；表空间约 220 MiB。
- 50,000,000 行生成：已完成并提交；从 100 万续写耗时 1,970.406 秒，最终精确核验完成于 1,986.125 秒。
- 精确行数：50,000,000；`MIN(id)=1`；`MAX(id)=50,000,000`。
- 完成时统计：数据约 9,283,043,328 bytes，原索引约 1,487,929,344 bytes。
- 基线查询：已完成；时间单条件 23,514.547 ms，OR 29,596.964 ms，均为全表扫描。
- 候选索引创建：已完成；在线 DDL 用时 343.188 秒，保留全部原始索引。
- 优化查询：已完成；时间单条件 11.851 ms，联合 AND 0.927 ms，OR 102.736 ms。
- 最终验收：50,000,000 行、100,000 个 BatchCode、`CHECK TABLE QUICK` 状态 OK，表空间约 12.62 GiB。

## Data_Batch 与互斥查询改造

- 在压力测试库新增 `Data_Batch`，仅含 `BatchCode VARCHAR(255) NOT NULL`。
- 按生产约束未给 `Data_Batch` 创建任何索引；`SHOW INDEX` 返回 0 行。
- 从 `Data_Product` 聚合写入前 1000 个不同批次号：1000 行、1000 个不同值，耗时 0.244 秒。
- QUERY_WEB 查询页改为“按时间/按批次号”互斥模式，批次下拉一次读取 `Data_Batch.BatchCode`。
- Group 配置新增 `batch_field`，压力测试配置将 `Data_Product` 映射为 `BatchCode`。
- 未修改 `sma_data_test1`，未修改压力测试库中的其他表。

## 批次号来源可配置改造

- View 增加 `batch_source.table/field`，不再由代码固定读取 `Data_Batch.BatchCode`。
- 配置页增加来源表、来源字段联动下拉和独立保存按钮。
- 查询页按 `view_name/group` 请求批次号，前端不传数据库标识符。
- 压力库标准来源 `Data_Batch.BatchCode` 读取1000条约39.813ms。
- 使用数据库会话临时表 `ProductionOrderTemp(OrderNo)` 验证不同表名/字段名，去重排序结果为 `PO-001`、`PO-002`，约1.347ms；连接释放后临时表自动消失。
- 浏览器中将来源保存为 `Data_Product.BatchCode` 后，查询页成功加载1000个批次号；测试使用隔离 profile，未改变现场激活配置。

## 插件查询页同步改造

- 普通插件页增加“按时间/按批次号”互斥选择，直接复用插件绑定 View/Group 的批次来源和目标字段配置。
- 普通插件页改为 `sort_by + id` 游标前后翻页，每次读取 `page_size + 1` 行且不执行总数统计；`cursor` 仍专用于 OPC UA 行选择，翻页游标使用独立 `page_cursor`。
- 高级 OPC UA 模式保留原 OFFSET、总页数和 PLC 页码驱动行为，人工时间/批次筛选禁用。
- 压力库浏览器验收：`Data_Batch.BatchCode` 加载1000个选项；时间和批次查询均返回50行；批次第二页与第一页 ID 重复数为0。
- 根目录 `docs/022-✅-QUERY_WEB五千万行游标查询适配.md` 和对应 `todo.md` 条目已恢复到查询适配开始前状态，后续记录只保存在用户允许的本项目与 `_Doc` 目录。
