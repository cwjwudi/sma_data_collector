# 版本发布记录（CHANGELOG）

> **Report Editor** 桌面版 semver 变更的**唯一完整记录**。发版、写 `latest.json`、对外说明时以本文为准。  
> 里程碑与工单规划见 [002_里程碑与工单.md](002_里程碑与工单.md)；打包步骤见 [packaging/README.md](../packaging/README.md)。

---

## 维护说明

每次发版请：

1. 在本文**顶部「详细记录」**新增一节（最新版本在最上）。
2. 更新下方**版本一览表**的状态与日期。
3. 执行 `node packaging/scripts/bump-version.mjs <版本> --notes "..."` 同步 `frontend/package.json` 与 `packaging/updates/latest.json` 占位。
4. 打包完成后用 `generate-update-manifest.mjs` 生成 SHA256，写入 `packaging/updates/latest.json` 并同步 Portal（**校验值只维护在 manifest，不在本文重复**）。
5. 在 [002_里程碑与工单.md](002_里程碑与工单.md)「变更记录」追加一行摘要（可选，避免在 002 重复写全文）。

**状态图例：** ✅ 已发布 · 🟡 已打包待 Portal · 📝 开发中 / 草案

**详细记录写法：** 写**变更**与**关联里程碑**即可；安装包文件名、SHA256、大小以 Portal `latest.json` 为准，不在此重复。

**用户数据目录（安装版）：**

| 系统 | 路径 |
|------|------|
| Windows | `%APPDATA%\sd-sma-report-editor\backend-data\` |
| macOS | `~/Library/Application Support/sd-sma-report-editor/backend-data/` |

---

## 版本一览

| 版本 | 状态 | 发布日期 | 摘要 |
|------|------|----------|------|
| **0.1.26** | 📝 | 2026-07-04 | 模版管理与版式页打开提速：默认列表模式并记忆、keep-alive、缩略图懒渲染、批量拉取/会话缓存复用、后端摘要 sidecar |
| **0.1.25** | ✅ | 2026-07-01 | 修复启动白屏；Windows 完整更新+说明；OPC UA 全局截批（Win 已重打包上传） |
| **0.1.24** | 📝 | 2026-06-25 | 删除确认焦点修复；用于验证 Windows 增量推送流程 |
| **0.1.23** | 📝 | 2026-06-25 | Windows 增量更新；OPC UA 结构体显示修复；Windows 启动修复 |
| **0.1.22** | 📝 | 2026-06-17 | 合并 Wenjie data-report：分文件导出、标量 SQL、自动导出命名；便携包含 Report Copy |
| **0.1.21** | ✅ | 2026-06-17 | 设置页证书验证；Mac/Win 双平台已发布 |
| **0.1.20** | ✅ | 2026-05-29 | 侧边栏可收起；圆形更新控件；导航显示版本号 |
| **0.1.19** | 🟡 | 2026-05-28 | 侧边栏底部更新进度/暂停/安装；打包 --version 校验（建议用 0.1.20 包） |
| **0.1.18** | ✅ | 2026-05-28 | 封面/封尾可选；截批/模拟截批；生成报表用语；Mac/Win 双平台 |
| **0.1.17** | ✅ | 2026-05-28 | 封面/末页预览显示画布控件；Mac arm64 已打包 |
| **0.1.16** | ✅ | 2026-05-28 | 刷新架构修复；Mac/Win 双平台 |
| **0.1.15** | ✅ | 2026-05-25 | 启动与数据源稳定性；0.1.14 安装包补丁 |
| **0.1.14** | ✅ | 2026-05-25 | 演示启停、审计增强、连接可观测、Win 样式修复 |
| **0.1.13** | ✅ | 2026-05-25 | 演示双通道、审计 MVP、Mac/Win 双平台发布 |
| **0.1.12** | ✅ | 2026-05-22 | 探活、测试写回 PLC、写回可观测、配置备份加固 |
| **0.1.11** | ✅ | 2026-05-22 | Win 升级保留配置；导出反馈 OPC 绑定与类型筛选 |
| **0.1.10** | ✅ | 2026-05-22 | PDF/导出可读错误；OPC 写回 PLC；导出前连接检查 |
| **0.1.9** | ✅ | 2026-05-22 | Win 更新下载修复；每小时检查；下载速度/ETA |
| **0.1.8** | ✅ | 2026-05-22 | 断点续传；跳过版本；Mac 装后打开 |
| **0.1.7** | ✅ | 2026-05-22 | 连接测试可读错误；更新 SHA256/ Mac 引导；Mac 诊断文案 |
| **0.1.6** | ✅ | 2026-05-21 | 软件更新自动检查/红点；模版版式云端同步 |
| **0.1.5** | ✅ | 2026-05-21 | 表格列宽可视化拖拽 |
| **0.1.4** | ✅ | 2026-05-21 | 版式云端同步；更新文案优化 |
| **0.1.3** | ✅ | 2026-05-21 | 软件更新面向用户的说明与错误文案 |
| **0.1.2** | ✅ | 2026-05-21 | Win/macOS 图标修复；双平台更新清单 |
| **0.1.1** | ✅ | 2026-05-21 | 应用内检查更新；设置页软件更新；latest.json 流程 |
| **0.1.0** | ✅ | 2026-05-21 | 首个 Win NSIS + Mac DMG 可安装包 |

---

## 详细记录

### 0.1.26 📝

**主题：** 模版管理页打开速度优化

**发布日期：** 2026-07-04（开发中）

**背景：** 模版管理默认「缩略图」模式，一进页面就串行拉取全部版式 + 每份完整模版 JSON（单文件约 450–560KB，含大量 base64 图片），并一次性渲染多份微缩预览，导致打开缓慢。

**变更：**

**模版管理页（TemplateManager）：**

- **默认列表模式并记忆**：首次打开即时呈现；用户切换的视图模式记入 `localStorage`（键 `tm-view-mode`），下次沿用
- **页面 keep-alive**：`TemplateManager` 纳入 `<keep-alive>`，改用 `onActivated` 加载，切走再切回「秒显示 + 后台仅刷新有变更的模版」
- **缩略图懒渲染**：用 `IntersectionObserver` 只为进入（或接近）视口的卡片构建微缩预览重 DOM，未入视口用等高占位
- **批量拉取**：首次进入缩略图模式改用 `/templates/full` 一次拉取，替代 N 次逐个 GET；失败回退逐个并发
- **去除多余深拷贝**：网络/本地读取到的模版对象本就是新解析结果，跳过 `cloneDeepTemplate`，省一半解析与内存
- **后端摘要 sidecar**：`{id}.meta.json` 保存轻量摘要，`GET /templates` 列目录时优先读 sidecar（mtime 不早于主文件才采用），避免解析整份大模版；保存/删除时同步维护，首次列目录自动回填

**版式与页眉页脚页（LayoutPresets）：** 同因默认缩略图 + 每次 `/layout-presets/full` + 一次性渲染全部预览而变慢，套用同款优化：

- **默认列表模式并记忆**（键 `lp-view-mode`）、**页面 keep-alive + `onActivated`**
- **缩略图懒渲染**：`IntersectionObserver` 只渲染进入视口的 `LayoutPresetMiniPage`
- **会话缓存复用**：进入页面用 `ensureLayoutPresetsLoaded()` 复用内存快照，不再每次都请求 `/layout-presets/full`（增删改仍强制刷新）；与模版管理页共享同一快照，两页互相进入更快

**说明：** 本版仅优化「打开/加载」路径，未改动模版/版式数据格式；图片外置与预生成静态缩略图（更彻底的体积优化）留待后续单独迁移评估。

**关联里程碑：** M7（体验）

---

### 0.1.25 📝

**主题：** OPC UA 自动截批全局监听

**发布日期：** 2026-07-01（Windows x64 已打包；macOS arm64 沿用同版本 dmg 清单）

**变更：**

- **生成报表**：将 OPC UA 自动截批轮询提升至应用级（`MainLayout`），离开「生成报表」页后仍持续监听触发变量并保存 PDF
- **体验**：后台触发成功时弹出 Toast；触发记录写入 localStorage，回到生成报表页可查看
- **说明**：「模拟截批」（手动选文件夹）仍须在生成报表页操作
- **Windows 更新**：检查更新时从 `latest.json` 读取并展示更新说明；保留 NSIS 差分更新（增量优先，可选完整安装包）
- **修复**：`MainLayout` 缺少 `loadSidebarCollapsed` 导入导致 0.1.25 启动白屏（Mac/Windows 均受影响，与差分更新无关）

**若已因 0.1.25 白屏：** 请从 Portal 手动下载对应平台安装包重新安装（会保留用户数据目录下的配置）。

**下载：** Portal `downloads/report-editor/`（Win ✅；Mac 清单已保留，安装包按既有 dmg 发布）

**关联里程碑：** M7（体验）

---

### 0.1.24 📝

**主题：** 删除确认焦点修复与增量更新验证

**发布日期：** 待打包（Windows 优先，用于验证增量推送）

**变更：**

- **焦点修复**：将模版、版式、签名库、历史报表等删除确认从原生 `window.confirm()` 改为应用内确认弹窗，避免 Electron 下确认框关闭后输入框无法继续输入
- **焦点兜底**：为残留原生 `confirm/alert` 增加关闭后的窗口焦点恢复，降低系统对话框造成的键盘焦点异常
- **发布验证**：作为 0.1.24 打包版本，用于测试焦点问题修复与 Windows 增量更新推送链路

**下载：** Portal `downloads/report-editor/`（待打包）

**关联里程碑：** M7（体验）、M9（应用内更新）

---

### 0.1.23 📝

**主题：** Windows 增量更新与 OPC UA 结构体显示修复

**发布日期：** 待打包（Windows 优先；macOS 维持全量 dmg）

**变更：**

- **Windows 更新**：接入 `electron-updater` NSIS 差分下载，优先使用 `latest.yml` 与 `.blockmap` 进行增量下载
- **兜底下载**：设置页保留“下载完整安装包”，差分不可用时也可直接下载完整 Setup.exe
- **发布流程**：Portal 同步 Windows `latest.yml`、安装包 `.blockmap` 与完整安装包；macOS 继续使用现有 `latest.json` + dmg 流程
- **OPC UA**：修复报表中结构体数据无法展开/显示的问题
- **Windows 启动**：修复 0.1.23 包启动时报 `builder-util-runtime` 缺失的问题

**下载：** Portal `downloads/report-editor/`（待打包）

**关联里程碑：** M9（应用内更新）

---

### 0.1.22 📝

**主题：** 合并 Wenjie `feature/temp_report`（data-report 与报表复制）

**发布日期：** 待打包（建议 Mac/Win 双平台一并发 **0.1.22**）

**变更：**

- **生成报表**：支持分文件 PDF 导出、标量 SQL 查询、自动导出目录与文件名（含 OPC UA 变量片段）
- **模版**：页眉页脚/表格 SQL 填充增强；导出前预检与 OPC 截批反馈可按模版绑定
- **便携运行时**：`_Launcher` 打包纳入 `SD_SMA_REPORT_COPY`（8094 报表复制 Web）
- **开发**：新增 `scripts/dev/windows/start_dev_electron.bat`

**下载：** Portal `downloads/report-editor/`（待打包）

**关联里程碑：** M7（体验）、data-report

---

### 0.1.21 ✅

**主题：** 设置页证书验证

**发布日期：** 2026-06-17（macOS arm64、Windows x64；Portal `latest.json` 已含双平台 SHA256）

**变更：**

- **设置**：底部「证书验证」，输入有效许可证可累加授权天数（演示状态展示，不限制功能）

**下载：** Portal `downloads/report-editor/`（Mac ✅，Win ✅）

**关联里程碑：** M7（体验）

---

### 0.1.20 ✅

**主题：** 侧边栏收起与更新交互优化

**发布日期：** 2026-05-29（macOS arm64、Windows x64）

**变更：**

- **导航栏**：支持收起为仅图标（64px）；收起时隐藏「报表编辑器」标题，**只显示版本号**；展开恢复完整菜单；状态写入 `localStorage`
- **软件更新**：展开时底部保留进度条、暂停、立即安装；**收起时**为圆形进度钮，外圈进度、悬停切换暂停/继续/安装图标
- **延续 0.1.19**：标题区显示当前版本；设置页与侧边栏共用下载状态

**下载：** Portal `downloads/report-editor/`（待打包）

**关联里程碑：** M7（体验）

---

### 0.1.19 🟡

**主题：** 侧边栏更新区与打包版本校验（代码已合入 `main`，建议与 0.1.20 一并打安装包）

**发布日期：** 2026-05-28（未单独发 Portal 安装包时，以 **0.1.20** 为准）

**变更：**

- **导航栏**：标题后显示当前版本；下载新版本时底部显示进度条、暂停、立即安装
- **打包**：`build.sh` / `build.ps1` 支持 `--version` / `-Version`，产物文件名与 `package.json` 强校验

**关联里程碑：** M7

---

### 0.1.18 ✅

**主题：** 封面/封尾可选、截批语义与双平台发版

**发布日期：** 2026-05-28（macOS arm64、Windows x64；Portal `latest.json` 已含双平台 SHA256）

**变更：**

- **模版**：未选用封面/封尾时不再导出空白页；「不使用封面/封尾」与缩略图「未选用」占位一致；取消版式后清空残留，修复编辑器白屏
- **生成报表**：页面用语统一为「模拟截批」「OPC UA 自动截批」「截批结果反馈」；PLC 信息节点成功时仍写 `模拟截批` / `截批`（自动）
- **打包**：`packaging/mac/build.sh` 版本/manifest 校验、单测门禁、`--only mac` 分平台同步；Windows `build.ps1` 产出 0.1.18 x64 Setup

**下载：** Portal `downloads/report-editor/`（Mac ✅，Win ✅）

**关联里程碑：** M7（体验）

---

### 0.1.17 ✅

**主题：** 模版封面/末页预览与编辑一致

**发布日期：** 2026-05-28（macOS arm64 已打包）

**变更：**

- **模版预览**：修复封面、末页上编排的画布控件在模版管理缩略图、导出预览中不显示的问题（`TemplateMiniPage` 现与编辑画布一致渲染 `coverElements` / `backElements`）

**下载：** Portal `downloads/report-editor/`（Mac ✅）

**关联里程碑：** M7（体验）

---

### 0.1.16 ✅

**主题：** 数据源「刷新架构」修复

**发布日期：** 2026-05-28（macOS arm64、Windows x64）

**变更：**

- **数据源**：修复数据库工作台点击「刷新架构」后表与视图被清空的问题（先拉库列表，再拉当前库的表/视图）

**下载：** Portal `downloads/report-editor/`（Mac ✅，Win ✅）

**关联里程碑：** M2/M3（数据源）

---

### 0.1.15 ✅

**主题：** 启动与数据源稳定性（0.1.14 安装包补丁）

**发布日期：** 2026-05-25（macOS arm64、Windows x64）

**变更：**

- **启动体验**：Electron 先开窗再等待后端；检测并清理占用 `:8000` 的僵死 `report_backend` 进程；健康后端可复用
- **数据源快速展示**：主进程 `config.json` 启动快照 IPC；DB 工作台本地架构缓存（`localStorage`）；表/视图后台探活不阻塞 UI
- **OPC UA**：修复保存连接时 `apiFetch is not defined`；OPC 列表本地快照先显示
- **云端同步**：模版/版式下载 TLS/网络错误重试与中文友好提示（`layout-sync.cjs`）

**下载：** Portal `downloads/report-editor/`（Mac ✅，Win ✅）

**关联里程碑：** M7（体验）、M2/M3（数据源）、M+-12 启动快照

**下一版本建议：** **0.1.16+** → M13（签章/PDF 一致）→ M10（加密配置包）

---

### 0.1.14 ✅

**主题：** 演示好用 + 审计好用 + 连接看得见

**发布日期：** 2026-05-25（macOS arm64、Windows x64）

**变更：**

- **M11**：演示与培训 Windows 样式修复；应用内一键启动/停止本地演示工具包（Docker compose）
- **M14**：操作审计高级筛选（类型/结果/日期）、CSV 导出、90 天保留策略；仪表盘与侧边栏「操作审计」入口（Win 0.1.13 打包遗漏）
- **M12**：Dashboard 连接探活汇总（DB + OPC）；数据源 Tab 点击「N 异常」查看详情弹窗

**下载：** Portal `downloads/report-editor/`（Mac ✅，Win ✅）

**关联里程碑：** M11-T6、M14 收尾、M12 小收尾

---

### 0.1.13 ✅

**主题：** 可验证 + 可追溯

**发布日期：** 2026-05-25（macOS arm64、Windows x64）

**变更：**

- **Mac 补丁（F1）**：升级页「打开已安装的应用」修复 `object could not be cloned`
- **演示双通道（M11 MVP）**：设置 → 演示与培训；远程/本地通道；健康检查；一键添加仿真连接；可选 Portal 演示工具包下载
- **操作审计（M14 MVP）**：JSONL 存储；独立页面列表/筛选/导出；关键路径 `auditLog` 埋点
- **数据源 Tab**：仿真连接显示「仿真」后缀

**下载：** Portal `downloads/report-editor/`（Mac ✅，Win ✅）

**关联里程碑：** M11（MVP）、M14（MVP）、M7

---

### 0.1.12 ✅

**状态：** macOS arm64、Windows x64 均已发布（Portal）

**发布日期：** 2026-05-22

**变更：**

- **连接 Tab 错误详情（T1）**：数据库/OPC 连接 Tab 悬停显示最后一次探活/测试的错误信息与时间
- **可选定时探活（T2）**：设置 →「连接定时探活」开关（默认关）与间隔（10–3600 秒）；开启后主导航与数据源页按间隔轮询
- **导出反馈「测试写回 PLC」（T3）**：生成报表页按钮，对已绑定节点写入成功态测试值
- **写回失败可观测（T4）**：手动/自动导出后写回失败 Toast + `manualHint` / `autoStatus` 摘要
- **绑定校验按已配置项（T5）**：手动导出前仅校验已填 NodeId；不要求三节点齐全
- **配置备份加固（T6）**：设置页置顶备份说明与醒目 callout；备份含生成报表偏好
- **自动导出写回（T7）**：自动导出路径沿用 `notifyExportResultToPlc`，失败摘要写入 `autoStatus`

**关联里程碑：** M12（部分）、M7

---

### 0.1.11 ✅

**状态：** macOS arm64、Windows x64 均已发布（Portal）

**发布日期：** 2026-05-22

**变更：**

- **修复 Windows 应用内升级误删用户配置**：NSIS 覆盖升级时 `${isUpdated}` 为 true 不再删除 `%APPDATA%\sd-sma-report-editor\`；`deleteAppDataOnUninstall` 改为 false
- **修复「导出结果反馈」OPC UA 变量树选择失效**：打开绑定弹窗前刷新已保存连接；弹窗 remount 时正确加载连接列表；绑定树/搜索按类型筛选（Boolean / Int / String）；绑定后显示 NodeId 与变量名
- **设置 → 连接偏好**：展示用户数据目录（`backend-data`），Electron 下可在文件管理器中打开
- **软件更新**：升级前确认框提示先导出配置备份；启动时清理 `%TEMP%\report-editor-updates\` 历史安装包
- **打包**：`electron-builder` 输出路径由 `../../packaging` 修正为 `../packaging`（避免误写到 `_Prj/packaging/`）
- **数据源配置**：Tab 增加连接状态灯与「· N 异常」计数（M12 小切片）
- **Windows 打包**：`build.bat` 成功不再 pause；支持 `-NoPause`

**关联里程碑：** M12-T5（部分）

---

### 0.1.10 ✅

**发布日期：** 2026-05-22  
**Git：** `0d2ce40`

**变更：**

- PDF 导出：失败时显示可读错误与操作建议（渲染超时、磁盘、权限、数据源等）
- 手动导出前检查模版用到的数据库 / OPC UA 连接，失败时可选择取消或继续
- PDF 渲染前检测绑定填充错误，避免静默导出带错误占位的 PDF
- 自动导出失败时全局 Toast 提示（任意页面可见），并写入绑定触发历史
- **导出结果 OPC UA 写回 PLC**：「生成报表」页可配置状态（Boolean/Int）、信息（String）、路径（String）节点；手动与自动导出完成后写入，供 PLC 读取

**后端：** `POST /opcua/write_saved/{server_id}`

**关联里程碑：** M12 小切片

**已知问题（已在 0.1.11 修复）：** Windows 应用内升级会误删 AppData 中的配置与模版。

---

### 0.1.9 ✅

**发布日期：** 2026-05-22  
**Git：** `4c7a586`

**变更：**

- 修复 Windows 应用内更新下载失败（安装包文件名 URL 编码 `%20` 未解码导致 EPERM）
- 软件更新：启动后及**每小时**自动检查；下载显示速度、大小、已用/剩余时间
- 下载/安装失败时显示可读错误与操作建议（权限、磁盘、网络、杀毒软件等）
- 高级设置：**清除已跳过版本**，清除后重新检查可再次发现该版本

**关联里程碑：** M9 完成

---

### 0.1.8 ✅

**发布日期：** 2026-05-22  
**Git：** `afc958b`

**变更：**

- 软件更新：下载支持**断点续传**（暂停后可「继续下载」）
- macOS：一键升级后自动退出；可选「替换完成后尝试自动打开应用」
- 软件更新：启动弹窗与设置页可「**跳过此版本**」；下载进度显示百分比
- 设置页：macOS 升级步骤更新；「打开已安装的应用」快捷按钮

**关联里程碑：** M9-T6

---

### 0.1.7 ✅

**发布日期：** 2026-05-22  
**Git：** `c37bf54`

**变更：**

- 修复 macOS 安装版「运行环境诊断」误显示为 Windows 安装版
- 软件更新：SHA256 校验失败提示更清晰；显示更新源与上次检查时间；macOS 升级分步引导
- 数据库 / OPC UA **连接测试**失败时显示更可读的原因与操作建议
- 设置与签名库：说明「图像签章」与 PDF 数字证书签名的区别

**关联里程碑：** M9-T5；M12 连接错误（部分）

---

### 0.1.6 ✅

**发布日期：** 2026-05-21  
**Git：** `f5661ea`、`115eea4`、`9b2520f`

**变更：**

- **软件更新**：启动静默检查并弹窗；设置入口红点；下载在主进程且切页不中断；可暂停下载
- **模版与版式云端同步**：Portal 登录后上传/下载；`templates/full` 与 `import-bulk`；团队文件 `team-templates.json`

**关联里程碑：** M9-T1～T4；版式同步

---

### 0.1.5 ✅

**发布日期：** 2026-05-21  
**Git：** `b59c00b`

**变更：**

- 属性面板表格**列宽改为可视化拖拽**（`TableColumnWidthVisualEditor`），与画布一致
- 修复表格列宽百分比输入无法正常修改（`ecac969`）
- 表格填充色作用于单元格内侧（`8c20caf`）
- 表格单元格/列填充；修复预览双击编辑卡死

---

### 0.1.4 ✅

**发布日期：** 2026-05-21  
**Git：** `8f61ebb`、`6557fae`

**变更：**

- **版式云端同步**（Portal 登录上传/下载）
- 软件更新用户文案优化

---

### 0.1.3 ✅

**发布日期：** 2026-05-21  
**Git：** `4c7bf23`

**变更：**

- 设置页软件更新说明改为面向终端用户
- 错误提示不再暴露技术路径

---

### 0.1.2 ✅

**发布日期：** 2026-05-21  
**Git：** `812862a`

**变更：**

- 修复 Windows / macOS **应用图标**（`icon.ico` + rcedit）
- 更新清单同时支持 Windows 与 macOS 平台条目
- 修复 Windows 打包 winCodeSign 符号链接问题（afterPack + rcedit）
- 修复 Windows 上 vite build 内存不足（`NODE_OPTIONS` 提堆）

---

### 0.1.1 ✅

**发布日期：** 2026-05-21  
**Git：** `e51f4f9`

**变更：**

- **应用内检查更新**：设置页「软件更新」、`electron/updater.cjs`
- Portal 静态目录 `latest.json` 发布流程
- 修复打包遗漏 `updater.cjs`

---

### 0.1.0 ✅

**发布日期：** 2026-05-21（约）

**变更：**

- 首个可现场交付的 **Windows NSIS** 与 **macOS DMG** 安装包
- PyInstaller 后端纳入 Electron `extraResources`
- 用户数据与程序安装目录分离（`backend-data`）
- 应用内在线更新尚未完整（自 0.1.1 起）

**关联里程碑：** M8 打包发布

---

## 变更记录（本文档）

| 日期 | 内容 |
|------|------|
| 2026-05-25 | **0.1.15** 标已发布（Mac/Win 双平台 + Portal `latest.json`） |
| 2026-05-25 | **0.1.14** 标已发布（Mac/Win + Portal）；新增 **0.1.15** 启动与数据源稳定性补丁规划 |
| 2026-05-25 | 修正 0.1.8 发布日期（2026-05-19 为规划草案日，实际打包/提交为 2026-05-22） |
| 2026-05-22 | 创建 007，汇总 0.1.0～0.1.11 发布记录 |
| 2026-05-22 | 精简格式：SHA256/安装包明细改由 latest.json 维护 |
