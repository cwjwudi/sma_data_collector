# 0.3.111 Plan

- **状态**：📝 开发中
- **基线**：0.3.110
- **任务看板**：[docs/030-🚧](../../../../docs/030-🚧-ReportEditor结批占满CPU导致mappView白屏.md)
- **对应发布记录**：[../007_版本发布记录.md](../007_版本发布记录.md) · 0.3.111

## 目标

分卷导出时禁止每份重复全量 SQL，并只渲染当前 `reportPartIndex`，降低同机 i3/Hypervisor/mappView 场景的 CPU 与 DB 压力（对齐 023）。

## 范围

- [x] `pdf-export-fill-cache`：part0 快照，part>0 复用；prewarm 清空
- [x] `buildExportPreviewReports`：导出只算当前份
- [x] `PdfExportView` / `TemplateExportPreviewStack` 接线
- [x] 单测 + bump **0.3.111**

## 不做

- 更换 printToPDF 引擎
- 报表机与 HMI 分机
