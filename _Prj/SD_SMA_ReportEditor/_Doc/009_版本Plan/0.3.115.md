# 0.3.115 Plan

- **状态**：🚧 实现中
- **基线**：0.3.114（选型）/ 0.3.113（现网）
- **任务看板**：[docs/030-🚧](../../../../docs/030-🚧-ReportEditor结批占满CPU导致mappView白屏.md)
- **对应发布记录**：发版后补 [../007_版本发布记录.md](../007_版本发布记录.md)

## 目标

同机 **自动结批零闪且 HMI 可操作**：旁路 `printToPDF`（pdf-lib 主路径）+ **模版/版式字体检查** + **随包开源 CJK**；策略 **A**（先零闪，版式渐进对齐，开关可回滚）。

## 范围

### 字体（本轮先交付）

- [x] 扫描模版画布 + 页眉页脚区 `fontFamily`
- [x] 导出预检 warning：本机不可用 → 将回退随包 Noto
- [x] 版式编辑器保存时字体告警确认
- [x] 属性面板即时提示缺字体
- [ ] 安装包携带 Noto Sans SC（子集优先）；pdf-lib `embedFont`（`fetch-noto-sans-sc.mjs` 已备）
- [x] 禁止把雅黑/宋体打进安装包（文档约束）

### 引擎（分步）

- [ ] 偏好 `pdfExportEngine`: `chromium`（默认）| `pdf-lib`
- [ ] main 导出分支：pdf-lib 路径不创建隐藏窗 `printToPDF`
- [ ] 取数 / fill-cache / 分卷 / jobId / 进度 IPC **保留**
- [ ] 矢量绘制：先正文表+标题可出 PDF，封面/复杂控件渐进
- [ ] 自动结批走同一引擎开关

## 验收

- [ ] 预检对缺字体模版给出中文 warning
- [ ] `pdf-lib` 开关下自动结批：8k/≥4 份 mappView **零闪可操作**（硬）
- [ ] 开关回 `chromium` 可回滚
- [ ] 中文靠随包 Noto，不依赖现场雅黑

## 不做

- 分机 / mappView 绑核 / 导出互斥
- Playwright 等另一 Chromium
- 拷贝微软字体进包
- 一次对齐全部 DOM 1:1 版式（属策略 A 后续）
