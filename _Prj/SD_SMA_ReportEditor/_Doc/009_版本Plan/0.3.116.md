# 0.3.116 Plan

- **状态**：✅ 代码合入（手测 Win 仍开）
- **基线**：0.3.115
- **任务看板**：[docs/033-✅](../../../../docs/033-✅-ReportEditor同机优先pdf-lib缺fontkit导致结批失败.md)
- **对应发布记录**：[../007_版本发布记录.md](../007_版本发布记录.md) · 0.3.116

## 目标

修复现场同机优先自动结批 `embedFont` / fontkit 失败；字体属性显示真实默认名 **Noto Sans SC**；打包自动携带字体文件。

## 拍板（2026-07-22）

| 项 | 决议 |
|----|------|
| 默认字体 | **Noto Sans SC**，软件自带（extraResources） |
| Q2 | **A**：空值占位显示 `Noto Sans SC（默认）`，不写死进模版 |
| Q3 | 下拉置顶并标「默认」 |
| Q4 | 本版只修 fontkit；`ERR_FAILED` 加固 → **0.3.117** |
| 版本 | **0.3.116** |

## 范围

- [x] `@pdf-lib/fontkit` + `registerFontkit` before `embedFont`
- [x] 字体 UI 占位 / 置顶 / 文案
- [x] `prebuild` → `fonts:fetch` 拉 Noto OTF
- [x] 单测 + bump **0.3.116**

## 不做（已迁 0.3.117）

- 多绑定导出窗 `ERR_FAILED` 加固 → 见 [0.3.117.md](./0.3.117.md)
- pdf-lib 版式 1:1
