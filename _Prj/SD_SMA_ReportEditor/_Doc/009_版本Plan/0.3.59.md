# 0.3.59 Plan

- **状态**：已发版（实现完成）
- **基线**：现场复现于 **0.3.58**（面板已可见；空连接编辑时表单周期性清空）
- **任务看板**：[docs/004-✅-ReportEditor数据源UI修复.md](../../../../docs/004-✅-ReportEditor数据源UI修复.md)
- **对应发布记录**：[../007_版本发布记录.md](../007_版本发布记录.md) · 0.3.59

## 目标

修复「数据库工作台无连接时，新建连接表单一直刷新、输入被清空、光标丢失」。

## 范围（已完成）

- [x] 空列表重载不打断 `creatingNew` 草稿；API 返回 `[]` 后停止 loadWatch
- [x] ConnectionManager：creatingNew 下连续 null 不重置 draft
- [x] 回归测试（策略单测 + loading 闪烁保留输入）
- [x] bump **0.3.59**，Mac 包，文档收尾

## 验收

1. `(0/0)` 编辑表单不被周期清空。
2. 无周期性 loading 闪烁。
3. 配置导入 / 连接变更仍刷新。
4. `npm test` 274 passed（含本版回归）。
