# 0.3.62 Plan

- **状态**：已发版（实现完成）
- **基线**：0.3.61
- **任务看板**：[docs/006-🚧-ReportEditor-AI上游错误体验.md](../../../../docs/006-🚧-ReportEditor-AI上游错误体验.md)
- **对应发布记录**：[../007_版本发布记录.md](../007_版本发布记录.md) · 0.3.62

## 目标

1. LLM 上游错误默认展示中文短文案，不甩整段 JSON。  
2. 设置页澄清 ChatGPT 订阅 ≠ API 额度；换上游后模型不在列表时警告并可改选。  
3. 强化 `SYSTEM_PROMPT` 覆盖能力域必调工具；探活代码随发版线合入。

## 范围

### 做

- [x] `format_llm_upstream_error` + `_forward_llm` 接入 + 单测
- [x] AI 设置页提示与模型 mismatch UI
- [x] `SYSTEM_PROMPT` 能力域点名
- [x] 探活 H1 收尾为 ✅（代码此前已在主干）
- [x] bump **0.3.62**

### 不做

- 能力矩阵 A–N 端到端剧本与 UI reload 全审计（仍 ⌛️，后续切片）
- 自动在保存 Base URL 时强制改写模型（本版仅警告 + 一键改选）

## 验收

见看板已完成 H1；单测绿；Mac 包。
