设置页中的「运行环境诊断」：一键检查端口、目录、配置；一键修复安全项。

- **开发模式**（源码 + `backend/venv`）：检查 Python、venv、npm、端口 8000/5173、数据目录等；可尝试重装 venv。
- **安装版**（PyInstaller `report_backend.exe`）：仅检查内置后端、用户数据目录与端口；不要求本机 Python/Node/venv。
