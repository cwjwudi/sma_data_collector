# 002 · ReportEditor 表格系统评估与修复

> 2026-07-12 对 `_Prj/SD_SMA_ReportEditor/frontend/` 表格控件系统做的三路并行深度审计（几何度量层 / SQL 填充分页链路 / 组件层与重复），及据此规划的修复执行列表。
> 审计为静态代码求证（前端 node_modules 当时未装，未跑 vitest）；每条结论标 CONFIRMED（源码可判定）或 PLAUSIBLE（需运行验证）。
> 本机开发/测试：`npm install --prefix _Prj/SD_SMA_ReportEditor/frontend` 后 `npx vitest run <file>`（在 frontend 目录）。

## 系统范围

- **组件层**：`components/report-template/` — TemplateBodyCanvas.vue(2297)、TemplateElementProps.vue(1530)、LayoutPresetPaperCanvas.vue(1716)、ZoneTableStatic.vue(167)、TableColumnResizeGutters.vue(130)、TemplateTableSqlFillFields.vue(504)、TemplateTableSqlVisualPanel.vue(1650)
- **逻辑层**：`lib/report-template/` — model.ts(994)、layout-zone-element.ts(867)、table-cell-metrics.ts(369)、table-content-layout.ts(152)、table-sql-fill.ts(815)、table-sql-fill-preview.ts(529)、binding-preview-utils.ts(843)，及一批 table-sql-fill-*、scalar-sql-* 与约 20 个 `*.test.ts`

## 评级与总结

设计 B+ / 实现 B- / 完整度 C+。架构主干合理（预览=导出复用同一套 Vue 组件、异步竞态用 generation 兜住、底层度量数学正确），但存在三层系统性问题：(1) 预览取 1000 行/导出取全量造成分页份数「所见非所得」；(2) 表格模型复制成 Template 系与 Zone 系两套且已漂移；(3) 一批整类能力缺失（合并单元格、中间列增删、请求取消、跨份数据快照）。无致命崩溃 bug，但有多个导致数据错误或「打印与屏幕不一致」的正确性 bug。

---

# ⌛️ 未完成：P1-A 编辑器导出预览行数/份数与真实 PDF 对齐

- **问题（CONFIRMED）**：`table-sql-fill-preview.ts:267` `sqlFillQueryLimit` 在编辑器（`fullSqlFill=false`）取数上限 `min(maxRows,1000)`，仅真导出 `PdfExportView.vue:182` 传 `fullSqlFill:true` 取全量（默认 maxRows=2000，拆分场景 50000）。结果超过 1000 行时编辑器预览页数偏少；拆分场景 ≤1000 行被判「不拆、显示 1 份」，实际导出拆成 2+ 份。`splitReportCountForPreview` 系统性少报份数。
- **触发**：SQL 结果 >1000 行（默认阈值极易达到）。
- **修复方向**：预览取数上限与导出对齐；若为性能保留上限，须在预览显式提示「预览仅显示前 N 行 / 实际约 M 页 P 份」，不能静默少报。
- **验收**：`table-sql-fill-preview.test.ts` / `table-sql-fill-report-split.test.ts` 增补跨 1000 行的份数/页数用例，走 TDD。

# 🚧 进行中：P1-B 长内容 240px 静默裁剪 + 纵表 0 行渲染字面 "…"

**已完成（纵表/横表 0 行 "…"）**：`table-sql-fill-preview.ts` 的 `formatVerticalSqlFillCell`/`formatHorizontalSqlFillCell` 无数据兜底由 `return "…"` 改为留空（表头行返回列名，正文留空 NBSP）；加载中占位 "…" 由上游 `formatSqlFillTableCellPreview:140` 保留。TDD：`table-sql-fill-preview.test.ts` 新增 4 用例先红后绿；前端全套 233 passed。提交 20dcdd4。

**已完成（放开 240px 上限）**：用户决策＝放开上限 + 超页跨页断行。已把 `TABLE_CONTENT_ROW_HEIGHT_MAX_PX` 由 240 放开到整页界（A4 纵向 ≈ 1123px）：窄列长文本不再被静默裁到 240，内容可长到接近整页；既有行级分页（`buildLogicalRowSlicesForOverflow`）负责把整行移到下一页。TDD：`table-cell-metrics.test.ts` 新增用例先红后绿；前端全套 234 passed。提交见 todo。

**仍待做（真·行内跨页断行）**：单行内容本身超过一整页时的「同一逻辑行跨页拆分」尚未实现——现架构每行是原子 `<tr>`，拆开需改渲染层（`TemplateMiniPage` + 分页引擎），属较大特性，另立子任务。当前上界仅保证单行不超过一页（超一页的极端行仍会在该行内截断）。

---

# （原）P1-B 说明保留

- **240px 裁剪（CONFIRMED）**：`table-cell-metrics.ts:42` `estimateWrappedTextHeightPx` 用 `TABLE_CONTENT_ROW_HEIGHT_MAX_PX(=240)` 封顶，而 `model.ts:388` / `table-content-layout.ts:53` 调用时不传 maxRowHeightPx，恒用 240。窄列长文本 >240px 时行框只到 240、超出被裁、无溢出提示 → 内容丢失。
- **纵表 0 行 "…"（CONFIRMED 静态推演）**：`table-sql-fill-preview.ts:190` `formatVerticalSqlFillCell` 末尾 `return "…"`；`:303` 纵表 0 数据仍保留 `slotRows` 行；导出 refresh `silent:true` 使 `previewLoading=false` → 落到 "…"。纵向填充表筛选命中 0 条时 PDF 印出字面省略号。
- **修复方向**：内容感知行高按内容需要放开上限（或分行溢出而非裁剪）；纵表非 loading 且 0 数据时输出空串而非 "…"。
- **验收**：`table-cell-metrics.test.ts`、`table-sql-fill-preview.test.ts` 增补用例。

# 🚧 进行中：P2-A SQL 参数值前端拼接（方言转义已修，真参数化待后续）

- **问题（CONFIRMED）**：`binding-preview-utils.ts:473` `quoteSqlScalarValue` 仅 `'`→`''`，不转义反斜杠；`substituteScalarSqlParams` 把值插值进 SQL 后 POST 给 `/database/query/sql`，后端不再参数化。MySQL/MariaDB 默认反斜杠为转义符，正常值 `C:\batch\` 即让字符串未终止、查询失败；恶意 OPC 值可断出字符串。
- **已完成（方言正确转义）**：`quoteSqlScalarValue`/`substituteScalarSqlParams` 新增 `engine` 参数，`isMysqlFamilyEngine` 判定；MySQL/MariaDB 同时转义 `\` 与 `'`，Postgres/SQLite 标准字符串仅翻倍引号（反斜杠为字面量，不误转）。`useReportBindingPreview.ts` 在标量与表格填充两条替换路径按 `connectionId` 反查引擎传入。TDD：`scalar-sql-params.test.ts` 新增 5 用例先红后绿；前端全套 243 passed，vue-tsc 0 错误。提交见 todo。
- **仍待做（真参数化）**：值改为绑定参数下发（前端 `{{pN}}`→`%s`/`?` + 有序 params 数组，后端 `DbExecuteSqlRequest` 加 `params` 字段并走 `run_*_readonly(params=...)`）。后端 run_*_readonly 已支持 params（上轮 pk 过滤已用），但前端预览任务管线（`TableSqlFillPreviewTask.sql`/`SqlDedupeTask.sql`）需一并携带 params，属跨切面重构且需真库端到端验证，单列为后续。当前方言转义已闭合注入/断串风险。

# ✅ 已完成：P2-B 窄/多列表格列宽拖拽失效

- **问题（CONFIRMED 推导）**：`table-cell-metrics.ts:341` `applyTableColumnResizeDeltaPx` 在内侧宽 < 列数×26 时钳制值符号反转，末行 `Math.max(lo, round(w))` 把所有列强抬 26 破坏总宽守恒。净效果：30 列表格在窄于 788px 的正文/页眉里列宽拖拽静默失效。两画布共用同一函数同时中招。
- **修复**：可收缩量 `maxLeft/maxRight` 限为非负（`Math.max(0, widths[x]-lo)`）避免方向反转；各列均 ≤ 下限的退化区两侧可收缩量均为 0 → 返回 null（拖拽无从满足，而非产出垃圾）；末行改为只四舍五入，不对全列强制下限（触碰的两列已保证 ≥ 下限，未触碰列保持原值，守恒不破坏）。
- **验收**：`report-template.test.ts` 新增 3 用例（退化区返回 null / 守恒且只动相邻两列且方向不反转 / 过量拖拽钳制到下限），先红后绿；前端全套 233 passed。提交 d82bd57。

# ✅ 已完成（网格原语层）：P2-C 表格模型 Template 系 / Zone 系去重

- **问题（CONFIRMED）**：`model.ts`（Template*）与 `layout-zone-element.ts`（Zone*）约 11 对 1:1 平行复制（ensureTableGrid / intrinsicOuterHeight... / clampTableElementOuterSize 等）。**已漂移实证**：`intrinsicOuterHeightForZoneTable`（layout-zone-element.ts:536）对 SQL 填充有 `return chrome + rows*minH` 短路，`intrinsicOuterHeightForTemplateTable`（model.ts:368）无此短路 → 同一 SQL 填充表在正文与页眉页脚算出不同高度。
- **已完成（抽取共享核心）**：新增 `table-grid-core.ts`，抽出**行为一致**的网格原语——`TableGridElementLike` 结构接口 + 泛型单元格 `ensureTableGridCore<Cell>(el, hydrateCell)`、`clampTableRowsDim`/`clampTableColsDim`、`ensureTableColWidthsPxCore`、`ensureTableColBgColors`、`tableVerticalChromePxFor(padding)`、`tableColumnInnerWidthsPxCore(el, padding, ensureGrid)`。`model.ts` 的 `ensureTableGrid`/`ensureTableColWidthsPx`/`templateTableColumnInnerWidthsPx`/`templateTableVerticalChromePx`、`layout-zone-element.ts` 的 Zone 同名函数改为**薄委托**，仅注入各自 padding 常量（正文 4/4、版式 2/4）与单元格 hydrate。`ensureTableColBgColors` 上移核心、Zone 侧再导出保持既有导入路径不变。
- **刻意保留（记为后续）**：`intrinsicOuterHeight*` 与外框 clamp（`minOuterSizeForTable`/`clampTableElementOuterSize` 等）因 SQL 填充高度语义存在**历史漂移**，本轮**不合并**、维持各自行为——合并即改变可见高度，需产品决策 + 运行应用视觉核对，单列后续子任务。
- **验收**：抽取后网格原语两系共用同一实现；前端全套 **243 passed**（行为保持，测试无需改动）、`tsc --noEmit` 触及文件 0 错误（仅存量 tsconfig `baseUrl` 弃用告警，与本改动无关）。提交见 todo。

# ⌛️ 未完成：P3 度量副作用 / 性能防抖 / 高缩放亚像素

- **度量函数带副作用（CONFIRMED）**：`templateTableColumnInnerWidthsPx`(model.ts:340)、`intrinsicOuterHeightForTemplateTable`(model.ts:368) 内部调 `ensureTableGrid` 就地改 `el.tableCells` 等；computed/渲染期调用产生非用户 mutation，undo/redo 隐患；一次 `clampTableElementOuterSize` 重复 ensure 约 5 次。
- **拖拽每步全表重算（PLAUSIBLE）**：`TemplateBodyCanvas.vue:1173` deep watch 每个 pointermove 触发全表 SQL 列目录重建 + 所有表 clamp，未防抖。
- **高缩放亚像素丢失（CONFIRMED 推导）**：`TableColumnResizeGutters.vue:72` 每次 move 推进 lastClientX 不留余数，`round` 归零则位移永久丢失；2.8× 缩放下细调列宽推不动。
- **修复方向**：度量函数拆纯只读版 / ensure 结果缓存；deep watch 防抖；拖拽累积亚像素余数。

# ⌛️ 未完成：盲区类能力缺口（需求确认后再定，非 bug 修复）

- 合并单元格（rowspan/colspan）完全不存在（全前端无 merge 字段）。
- 行列只能末尾增删：`ensureTableGrid` 末尾补/截，缩列永远丢最后一列，无法删中间列/重排。
- 拆分导出无统一数据快照：每份重新全量查询、各份取数时刻不同，DB 变动时跨份内容不一致；无 AbortController。
- 超页大行被 `overflow:hidden` 裁剪无跨页断行；最多 50000 行全量进 DOM 无虚拟化。
- 静态表跨页不重复表头（`includeHeaderRow:false` 硬编码）；时区不换算（只删 +00:00 后缀）；无千分位；布尔/二进制无专门格式化。
- 拖拽调宽/调尺寸键盘不可达（把手 tabindex=-1）。

---

## 亮点（保留，不动）

1. 预览=导出复用同一套 Vue 组件（PdfExportView → TemplateMiniPage），版式/切分/文案天然一致。
2. 异步竞态用 generation 自增 + await 边界校验兜住乱序覆盖。
3. 列宽拖拽把手 TableColumnResizeGutters 真·共用；底层度量数学两画布共享。
4. 撤销/重做 debounce 320ms 深拷贝快照，一次拖拽塌缩为一步。
5. 行高求和整数化无浮点漂移；列宽整数化用最大余数法保证和恒等。

## 执行进度

- ✅ **P1-B 前半**（纵表/横表 0 行 "…"）——提交 20dcdd4。
- ✅ **P2-B**（窄表列宽拖拽）——提交 d82bd57。
- ✅ **P1-B 后半 · 放开 240px 上限**（放开到整页界 1123px）——见下方提交。
- ⌛️ **P1-B 续 · 真·行内跨页断行**（单行超一整页时拆分渲染）——较大特性，待排期。
- 🚧 **P1-A 预览/导出行数**——决策已定（保留后端 1000 + 只加提示）；逻辑核心 `sqlFillPreviewTruncationHint` 已完成提交 77859d5，**UI 横幅接入待做**（需运行应用视觉核对）。
- 🚧 **P2-A**（SQL 参数值）——**方言正确转义已完成**（关闭 MySQL 反斜杠注入/断串），提交见 todo；真参数化重构待后续。
- ✅ **P2-C 去重重构（网格原语层）**——抽 `table-grid-core.ts`，Template/Zone 两系委托复用；`intrinsicOuterHeight`/外框 clamp 的 SQL 填充高度漂移**刻意保留**为需决策的后续。见下方提交。
- ⌛️ **P2-C 续 · 对齐 intrinsicOuterHeight 漂移**、**P3 性能/副作用**、**盲区能力**（合并单元格/中间列增删等）——排队。

## 用户已决策（2026-07-12）

1. **超高内容（240px）＝放开上限 + 超页跨页断行**。已完成放开上限部分；行内跨页断行为后续子任务（见上）。
2. **P1-A 行数＝预览与导出都按「单报表上限（maxRows）」渲染，超出行数时预览给提示、不显示全量**。二次决策：**保留后端 1000 上限，只加提示**（不改后端、不取全量）。
   - **已完成（逻辑核心）**：`table-sql-fill-preview.ts` 新增 `sqlFillPreviewTruncationHint(fill, fetchedRowCount)`——预览取回行数触及上限（`min(maxRows,1000)`）时返回提示文案「预览仅显示前 N 行；单报表上限 maxRows；实际（份数/页数）以导出为准」，否则返回 null。TDD 4 用例先红后绿；提交 77859d5。
   - **待做（UI 接入）**：把该提示接到编辑器画布/属性面板作横幅或角标显示。需读取绑定预览的 `pv.dataRows.length` 传入本函数，插入点在 `TemplateBodyCanvas.vue`(2297 行) 或 SQL 填充配置面板 `TemplateTableSqlFillFields.vue`(504 行)；UI 改动需运行应用做视觉核对，未在无头环境硬做。
