# 002 · ReportEditor 表格系统评估与修复

> 2026-07-12 对 `_Prj/SD_SMA_ReportEditor/frontend/` 表格控件系统做的三路并行深度审计（几何度量层 / SQL 填充分页链路 / 组件层与重复），及据此规划的修复执行列表。
> 审计为静态代码求证（前端 node_modules 当时未装，未跑 vitest）；每条结论标 CONFIRMED（源码可判定）或 PLAUSIBLE（需运行验证）。
> 本机开发/测试：`npm install --prefix _Prj/SD_SMA_ReportEditor/frontend` 后 `npx vitest run <file>`（在 frontend 目录）。

## 系统范围

- **组件层**：`components/report-template/` — TemplateBodyCanvas.vue(2297)、TemplateElementProps.vue(1530)、LayoutPresetPaperCanvas.vue(1716)、ZoneTableStatic.vue(167)、TableColumnResizeGutters.vue(130)、TemplateTableSqlFillFields.vue(504)、TemplateTableSqlVisualPanel.vue(1650)
- **逻辑层**：`lib/report-template/` — model.ts(994)、layout-zone-element.ts(867)、table-cell-metrics.ts(369)、table-content-layout.ts(152)、table-sql-fill.ts(815)、table-sql-fill-preview.ts(529)、binding-preview-utils.ts(843)，及一批 table-sql-fill-*、scalar-sql-* 与约 20 个 `*.test.ts`

## 评级与总结

设计 B+ / 实现 B- / 完整度 C+。架构主干合理（预览=导出复用同一套 Vue 组件、异步竞态用 generation 兜住、底层度量数学正确），但存在三层系统性问题：(1) 预览取 1000 行/导出取全量造成分页份数「所见非所得」；(2) 表格模型复制成 Template 系与 Zone 系两套且已漂移；(3) 一批整类能力缺失（合并单元格、中间列增删、请求取消、跨份数据快照）。无致命崩溃 bug，但有多个导致数据错误或「打印与屏幕不一致」的正确性 bug。

---

# ✅ 已完成：P1-A 预览截断提示 UI（0.3.105；取数仍保留 1000 上限）

- **问题（CONFIRMED）**：编辑器预览 `sqlFillQueryLimit` 上限 `min(maxRows,1000)`，真导出可取全量 → 大结果时预览页数/份数偏少（静默「所见非所得」）。
- **决策**：保留预览 1000 上限，**显式提示**不以预览页数/份数为准。
- **已完成**：`sqlFillPreviewTruncationHint`（逻辑）+ **0.3.105** 属性面板横幅（`TemplateTableSqlFillFields` + `previewKey`）。
- **验收**：逻辑单测已有；UI 需本机打开带大结果 SQL 填充表点验横幅（非挡发版）。

# ✅ 已完成：P1-B 长内容 240px 静默裁剪 + 纵表 0 行 "…" + 行内跨页断行（0.3.108）

**已完成（纵表/横表 0 行 "…"）**：无数据兜底留空；加载中占位 "…" 保留。提交 20dcdd4。

**已完成（放开 240px 上限）**：`TABLE_CONTENT_ROW_HEIGHT_MAX_PX` 放开到整页界（≈1123px）；编辑画布仍用此上界。

**已完成（真·行内跨页断行 · 0.3.108）**：导出预览/PDF 路径下，单逻辑行高于当前页剩余（含超整页）时按与 `estimateWrappedTextHeightPx` 同源的视觉行拆成多段跨页续排；静态表 + SQL 填充（含纵表 `page_per_record`）；slice 增 `rowTextLineStart/End`；分页估高 uncapped。不拆表头；编辑画布不多页。TDD：`table-cell-metrics` / `table-content-layout` / `table-sql-fill-row-fragment`；前端全套 455 passed。

---

# （原）P1-B 说明保留

- **240px 裁剪（CONFIRMED）**：`table-cell-metrics.ts:42` `estimateWrappedTextHeightPx` 用 `TABLE_CONTENT_ROW_HEIGHT_MAX_PX(=240)` 封顶，而 `model.ts:388` / `table-content-layout.ts:53` 调用时不传 maxRowHeightPx，恒用 240。窄列长文本 >240px 时行框只到 240、超出被裁、无溢出提示 → 内容丢失。
- **纵表 0 行 "…"（CONFIRMED 静态推演）**：`table-sql-fill-preview.ts:190` `formatVerticalSqlFillCell` 末尾 `return "…"`；`:303` 纵表 0 数据仍保留 `slotRows` 行；导出 refresh `silent:true` 使 `previewLoading=false` → 落到 "…"。纵向填充表筛选命中 0 条时 PDF 印出字面省略号。
- **修复方向**：内容感知行高按内容需要放开上限（或分行溢出而非裁剪）；纵表非 loading 且 0 数据时输出空串而非 "…"。
- **验收**：`table-cell-metrics.test.ts`、`table-sql-fill-preview.test.ts` 增补用例。

# ✅ 已完成：P2-A SQL 参数值真参数化下发（0.3.104）

- **问题（CONFIRMED）**：原路径把 `{{pN}}` 插值为 SQL 字面量再 POST；方言转义已闭合 MySQL 断串/注入，但仍非驱动绑定。
- **已完成（方言正确转义）**：见历史；`quoteSqlScalarValue` + engine 透传保留作兼容/诊断。
- **已完成（真参数化 · 0.3.104）**：
  - 前端 `bindScalarSqlParams`：`{{pN}}`/`'{{pN}}'` → `%s`（MySQL/PG）或 `?`（sqlite）+ 有序 `params`；值不进 SQL 文本。
  - `useReportBindingPreview` 标量 `sqlTasks` 与表格 `fillTasks` 两条路径 POST `/database/query/sql` 携带 `params`。
  - 后端 `DbExecuteSqlRequest.params` + `query_sql` 转发 `run_*_readonly(..., params=)`。
  - TDD：`scalar-sql-params.test.ts`（bind 用例）、`modules/test_query_sql_params.py`。
- **现场/真库**：建议用含反斜杠的 OPC/字面量参数在真 MySQL 上点验一次预览（非挡发版）。

# ✅ 已完成：P2-B 窄/多列表格列宽拖拽失效

- **问题（CONFIRMED 推导）**：`table-cell-metrics.ts:341` `applyTableColumnResizeDeltaPx` 在内侧宽 < 列数×26 时钳制值符号反转，末行 `Math.max(lo, round(w))` 把所有列强抬 26 破坏总宽守恒。净效果：30 列表格在窄于 788px 的正文/页眉里列宽拖拽静默失效。两画布共用同一函数同时中招。
- **修复**：可收缩量 `maxLeft/maxRight` 限为非负（`Math.max(0, widths[x]-lo)`）避免方向反转；各列均 ≤ 下限的退化区两侧可收缩量均为 0 → 返回 null（拖拽无从满足，而非产出垃圾）；末行改为只四舍五入，不对全列强制下限（触碰的两列已保证 ≥ 下限，未触碰列保持原值，守恒不破坏）。
- **验收**：`report-template.test.ts` 新增 3 用例（退化区返回 null / 守恒且只动相邻两列且方向不反转 / 过量拖拽钳制到下限），先红后绿；前端全套 233 passed。提交 d82bd57。

# ✅ 已完成（网格原语层）：P2-C 表格模型 Template 系 / Zone 系去重

- **问题（CONFIRMED）**：`model.ts`（Template*）与 `layout-zone-element.ts`（Zone*）约 11 对 1:1 平行复制（ensureTableGrid / intrinsicOuterHeight... / clampTableElementOuterSize 等）。**已漂移实证**：`intrinsicOuterHeightForZoneTable`（layout-zone-element.ts:536）对 SQL 填充有 `return chrome + rows*minH` 短路，`intrinsicOuterHeightForTemplateTable`（model.ts:368）无此短路 → 同一 SQL 填充表在正文与页眉页脚算出不同高度。
- **已完成（抽取共享核心）**：新增 `table-grid-core.ts`，抽出**行为一致**的网格原语——`TableGridElementLike` 结构接口 + 泛型单元格 `ensureTableGridCore<Cell>(el, hydrateCell)`、`clampTableRowsDim`/`clampTableColsDim`、`ensureTableColWidthsPxCore`、`ensureTableColBgColors`、`tableVerticalChromePxFor(padding)`、`tableColumnInnerWidthsPxCore(el, padding, ensureGrid)`。`model.ts` 的 `ensureTableGrid`/`ensureTableColWidthsPx`/`templateTableColumnInnerWidthsPx`/`templateTableVerticalChromePx`、`layout-zone-element.ts` 的 Zone 同名函数改为**薄委托**，仅注入各自 padding 常量（正文 4/4、版式 2/4）与单元格 hydrate。`ensureTableColBgColors` 上移核心、Zone 侧再导出保持既有导入路径不变。
- **已对齐（0.3.106 · 决策=以正文为准）**：去掉 `intrinsicOuterHeightForZoneTable` 对 SQL 填充的 `chrome+rows*minH` 短路，与正文一样走内容感知行高；外框 clamp 横/纵表策略仍各自保留（与正文对称）。
- **验收**：抽取后网格原语两系共用同一实现；前端全套 **243 passed**（行为保持，测试无需改动）、`tsc --noEmit` 触及文件 0 错误（仅存量 tsconfig `baseUrl` 弃用告警，与本改动无关）。提交见 todo。

# ✅ 已完成：P3-A 度量函数副作用剥离（getter 只读化）

- **问题（CONFIRMED）**：`templateTableColumnInnerWidthsPx`/`intrinsicOuterHeightForTemplateTable` 及 Zone 同名函数、`minOuterSizeFor*`、`computeZoneTableContentRowHeightsPx` 等**纯读取几何的 getter** 内部调 `ensureTableGrid`/`ensureZoneTableGrid` 就地改 `el`（补齐 `tableColWidthsPx`/`tableColBgColors`、重塑 `tableCells`、写回 `tableRows/tableCols`）；被放进 Vue `computed`/渲染函数（如 `ZoneTableStatic.vue:54`、`TemplateMiniPage.vue`）后，**渲染期产生非用户 mutation**——undo/redo 隐患、一次 `clampTableElementOuterSize` 重复 ensure 约 5 次。
- **修复**：`table-grid-core.ts` 新增只读原语 `tableColumnInnerWidthsPxReadonly(el, padding)`——用行/列钳制值 + `tableColWidthsPx` 的**补齐副本**重算，绝不写回；删除已被取代的可变版 `tableColumnInnerWidthsPxCore`。`model.ts`/`layout-zone-element.ts` 的列宽 getter、`intrinsicOuterHeightFor*Table`、`minOuterSizeFor*Table`、`computeZoneTableContentRowHeightsPx`、废弃的 `minOuterHeightFor*ResizePx` 全部去掉 `ensure*`、改用 `clampTableRowsDim/clampTableColsDim` 取代 ensure 的归一。**真 mutator 保留 ensure**：`applyTableOuterHeight`、`clampTableElementOuterSize`/`clampZoneTableOuterSize`（仅纵表 SQL 同步一处）——重复 ensure 随 getter 只读化自然坍缩。
- **验收（TDD 先红后绿）**：新增 `table-metrics-readonly.test.ts` 5 用例——「空 `tableColWidthsPx`/`tableColBgColors` 调 getter 后不被补齐、`tableCells` 引用不变、结果与预先 ensure 后逐值一致」，先红（补齐到 4）后绿。前端全套 **248 passed**、`tsc --noEmit` 触及文件 0 错误。网格已一致（水合后常态）时结果与旧路径逐值相等，行为保持。提交见 todo。

# ✅ 已完成：P3-B 拖拽防抖 / 高缩放亚像素余数（0.3.105）

- **拖拽每步全表重算**：`TemplateBodyCanvas` / `LayoutPresetPaperCanvas` 可视化 SQL 列目录 deep watch 改为 **200ms 防抖**（首帧仍立即刷新）。
- **高缩放亚像素丢失**：`takeLayoutDeltaFromScreen` 累积布局像素余数，凑整再 emit；`TableColumnResizeGutters` 接入。TDD：`column-resize-pointer.test.ts`。
- **手感**：建议本机 2.8× 缩放拖列宽点验（非挡发版）。

# ⌛️ 未完成：盲区类能力缺口（需求确认后再定，非 bug 修复）

- 合并单元格（rowspan/colspan）完全不存在（全前端无 merge 字段）。
- 行列只能末尾增删：`ensureTableGrid` 末尾补/截，缩列永远丢最后一列，无法删中间列/重排。
- 拆分导出无统一数据快照：每份重新全量查询、各份取数时刻不同，DB 变动时跨份内容不一致；无 AbortController。
- 最多 50000 行全量进 DOM 无虚拟化（行内跨页断行已在 0.3.108 落地）。
- 静态表跨页不重复表头（`includeHeaderRow:false` 硬编码）；时区不换算（只删 +00:00 后缀）；无千分位；布尔/二进制无专门格式化。
- 拖拽调宽/调尺寸键盘不可达（把手 tabindex=-1）。

---

# ✅ 已完成：同页纵表 / 多表页底截断未续页（2026-07-22）

**现象**（冒烟竖版 Chromium 导出，页码 2/3＝封面+单正文+封尾）：正文底部纵表 SQL 填充（`y≈540`，`verticalMultiRecordMode=continue`）画出多条记录后在纸底被裁切，页脚文案叠在表上；**剩余行未出现在下一页续表**。对照：同页上方横表数据正常。

**关联**：P1-B（0.3.108）已做「单逻辑行超高 → 行内跨页断行」与 `bodyCards` 续页骨架；本案是 **表锚点靠近页底时整表溢出未落到续页卡**。

**根因**：
1. `sumTableRowHeightsPx` 在 `heights.length>0` 时忽略 `rowCount`，切片外框可能被撑破。
2. 锚点页放不下时 `chunks.length<=1` 会**清空 slice 退回单卡**，迷你页仍按原 `y` 画全表 → 裁切/叠脚。

**处理**：
- `sumTableRowHeightsPx` 按 `rowCount` 取前 N 行。
- 需要分页时禁止丢弃 slice；锚点过紧则 `hideOverflowSqlFillTable` 首卡藏表、续卡 `top=0` 排续表（Mini + layout-v2）。
- 单测：`table-sql-fill-multi-overflow.test.ts`。

**验收**：上述单测 ✅；冒烟竖版导出应出现 SQL 续表（总页 >3）且页底无裁切叠脚。装包：**0.3.134**。

---

## 亮点（保留，不动）

1. 预览=导出复用同一套 Vue 组件（PdfExportView → TemplateMiniPage），版式/切分/文案天然一致。
2. 异步竞态用 generation 自增 + await 边界校验兜住乱序覆盖。
3. 列宽拖拽把手 TableColumnResizeGutters 真·共用；底层度量数学两画布共享。
4. 撤销/重做 debounce 320ms 深拷贝快照，一次拖拽塌缩为一步。
5. 行高求和整数化无浮点漂移；列宽整数化用最大余数法保证和恒等。

## 执行进度

- ✅ **P1-B 前半**（纵表/横表 0 行 "…"）——提交 20dcdd4。
- ✅ **P2-B**（窄表列宽拖拽）——提交 d82bd57。
- ✅ **P1-B 后半 · 放开 240px 上限**（放开到整页界 1123px）。
- ✅ **P1-B 续 · 真·行内跨页断行**（0.3.108）——视觉行片段跨页；静态 + SQL。
- ✅ **P1-A 预览/导出行数**——保留 1000 + 提示；逻辑 `sqlFillPreviewTruncationHint` + **属性面板横幅**（0.3.105，`TemplateTableSqlFillFields`）。
- ✅ **P2-A**（SQL 参数值）——方言转义 + **真参数化**（0.3.104）：`bindScalarSqlParams` + `query_sql` params。
- ✅ **P2-C 去重重构（网格原语层）**——抽 `table-grid-core.ts`，Template/Zone 两系委托复用；`intrinsicOuterHeight`/外框 clamp 的 SQL 填充高度漂移**刻意保留**为需决策的后续。见下方提交。
- ✅ **P3-A 度量函数副作用剥离**——列宽/高度/最小尺寸 getter 只读化，渲染期不再 mutate el；`tableColumnInnerWidthsPxReadonly` 抽出、可变版删除。见下方提交。
- ✅ **P3-B 拖拽防抖/亚像素余数**（0.3.105）——可视化列目录 deep-watch 200ms 防抖；`takeLayoutDeltaFromScreen` 亚像素余数。
- ✅ **P2-C 续 · 对齐 intrinsicOuterHeight 漂移**（0.3.106）——以正文为准，去掉 Zone SQL `chrome+rows*minH` 短路。
- ⌛️ **盲区能力**（合并单元格/中间列增删等）——排队。
- ✅ **同页纵表页底截断未续页**（2026-07-22）——`sumTableRowHeightsPx` + 锚点过紧改续卡。

## 用户已决策（2026-07-12）

1. **超高内容（240px）＝放开上限 + 超页跨页断行**。已全部完成（0.3.108 行内断行）。
2. **P1-A 行数＝预览与导出都按「单报表上限（maxRows）」渲染，超出行数时预览给提示、不显示全量**。二次决策：**保留后端 1000 上限，只加提示**（不改后端、不取全量）。
   - **已完成（逻辑核心）**：`table-sql-fill-preview.ts` 新增 `sqlFillPreviewTruncationHint(fill, fetchedRowCount)`——预览取回行数触及上限（`min(maxRows,1000)`）时返回提示文案「预览仅显示前 N 行；单报表上限 maxRows；实际（份数/页数）以导出为准」，否则返回 null。TDD 4 用例先红后绿；提交 77859d5。
   - **已完成（UI 接入 · 0.3.105）**：`TemplateTableSqlFillFields` 顶部横幅（`previewKey` → `bindingPreview.values` → `sqlFillPreviewTruncationHint`）；正文/版式属性面板分别传 `tblfill:` / `ztblfill:`。
