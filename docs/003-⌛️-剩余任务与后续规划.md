# 003 · 剩余任务与后续规划（跨 001/002 汇总）

> **快照日期**：2026-07-12（索引正文仍以当日为准；其后新增看板见文末「后续登记」）。本文档是「未完成任务 + 后续规划」的**统一索引/路线图**，深细节引用
> [`001-🚧-安全与可靠性缺陷修复.md`](001-🚧-安全与可靠性缺陷修复.md) 与
> [`002-🚧-表格系统评估与修复.md`](002-🚧-表格系统评估与修复.md)，此处不复制正文。
> 每个 H1 是一条可执行/可验收的剩余子任务，按 CLAUDE.md 约定标 ⌛️/🚧。

---

## 当前进度快照（git）

- `origin/main` 基线 = **`ec49152`**（A 档全量校核 / P0 修复 / 全局 uv 环境 / AGENTS 规则，已合并）。
- 本地 **领先 12 个提交未推送**（表格系统 P1-B/P2-B/240px/P1-A逻辑/P2-A方言转义/P2-C去重/P3-A只读化 及其文档），最旧 `1923c42` → 最新 `7361549`。
- 工作区干净。**推送被本环境无 git 凭据阻塞**，需在本机手动 `git push`（见下第 1 条）。

## 分类速查（挑任务用）

| 分类 | 含义 | 涉及任务 |
| ---- | ---- | ---- |
| 🟢 本机可纯逻辑闭环（TDD） | 代码+单测在本机即可验收 | **P2-A 真参数化**的前后端代码（完全验收另需真库，见 🏭） |
| 🖥 需运行应用核对 | 要 `npm run electron:dev` 看视觉/手感 | P1-A 提示横幅接入、P1-B 行内跨页断行、P3-B 防抖/亚像素 |
| 🧭 需产品决策 | 改动会变可见行为或涉及需求取舍 | P2-C 续（高度漂移取哪版）、盲区能力（合并单元格等） |
| 🏭 需现场 / 真库 / 协调 | 依赖真实环境或多人协调 | C 档口令轮换、git 历史清洗、P2-A 端到端真库验证 |

## 建议推进顺序

1. **你手动 `git push`** 本地 12 个提交（立即，见第 1 条）。
2. **下个编码 session**：**P2-A 真参数化**——目前唯一「本机能写代码 + 单测闭环」的代码项。
3. **集中跑应用的一档**：P1-A 提示横幅 + P3-B 防抖/亚像素（都要 electron:dev，攒一起做省切换）。
4. **需你拍板**：P2-C 续（intrinsicOuterHeight 高度漂移以正文无短路 / 版式有短路 哪版为准）；盲区能力做不做、做哪些。
5. **现场窗口**：C 档口令轮换（高优先）+ git 历史清洗。

---

# ⌛️ 待办：推送本地 12 个提交到 origin

- **动作**：本机 `git push`（当前分支 `main`）。Agent 环境无凭据，仅能本地 commit。
- **内容**：`1923c42`(002 评估排期) → `7361549`(P3-A) 共 12 提交，均为 ReportEditor 表格系统修复/重构 + docs/002 + todo。
- **验收**：`git rev-list --count origin/main..HEAD` 归 0。

# 🚧 进行中：P1-A 预览截断提示 UI 横幅接入

- **已完成**：逻辑核心 `sqlFillPreviewTruncationHint(fill, fetchedRowCount)`（提交 `77859d5`），预览取回行数触及上限时返回提示文案。
- **待做**：把提示接到编辑器画布/属性面板作横幅或角标；插入点 `TemplateBodyCanvas.vue` 或 `TemplateTableSqlFillFields.vue`，需读 `pv.dataRows.length` 传入。
- **为何未做**：横幅位置/样式需运行应用视觉核对（🖥）。细节见 002 对应 H1。

# 🚧 进行中：P2-A SQL 参数值真参数化下发

- **已完成**：方言正确转义（提交 `0262de6`），已闭合 MySQL 反斜杠注入/断串。
- **待做**：值改为绑定参数——前端 `{{pN}}`→`%s`/`?` + 有序 params 数组；后端 `DbExecuteSqlRequest` 加 `params` 字段并走 `run_*_readonly(params=...)`（后端已支持 params，前端预览任务管线需一并携带）。
- **可闭环度**：前后端代码可在本机 TDD 单测（🟢）；但端到端要真 MySQL 才算完全验收（🏭）。属跨切面重构，风险中等。细节见 002 对应 H1。

# ⌛️ 未完成：P1-B 续 · 单行超页真·行内跨页断行

- 放开 240px 上限已完成（`6e7f792`）；**单行内容超过一整页时的拆分渲染**未做。
- 属渲染层特性，改动面大，需运行应用核对（🖥）。细节见 002。

# ⌛️ 未完成：P2-C 续 · 对齐 intrinsicOuterHeight / 外框 clamp 的 SQL 填充高度漂移

- P2-C 已把**网格原语层**去重（`table-grid-core.ts`，提交 `34b49a8`）；但 `intrinsicOuterHeightForZoneTable` 对 SQL 填充有 `return chrome + rows*minH` 短路、`intrinsicOuterHeightForTemplateTable` 无——**刻意保留未合并**。
- **卡点（需你决策 🧭）**：以「正文无短路」还是「版式有短路」为准？合并即改变同一 SQL 填充表在正文 vs 页眉页脚的可见高度，需视觉核对。细节见 002。

# ⌛️ 未完成：P3-B 拖拽 deep-watch 防抖 + 高缩放亚像素余数

- P3-A 度量只读化已完成（`7361549`）；剩下两项性能/手感：
  - `TemplateBodyCanvas.vue` deep watch 每个 pointermove 全表重算，未防抖。
  - `TableColumnResizeGutters.vue` 拖拽不留亚像素余数，2.8× 缩放下细调列宽推不动。
- 数学可单测，但「手感」需运行应用核对（🖥）。细节见 002。

# ⌛️ 未完成：盲区类能力缺口（需求确认后再定，非 bug）

- 合并单元格（rowspan/colspan）、行列只能末尾增删（无法删中间列/重排）、拆分导出无统一数据快照/无 AbortController、超页大行无虚拟化、静态表跨页不重复表头、时区不换算、无千分位、拖拽把手键盘不可达。
- 均需**需求确认**优先级后再排（🧭）。清单见 002 对应 H1。

# ⌛️ 未完成：C 档安全现场收尾（承接 001）

- **口令轮换（高优先 🏭）**：`Br54644800@` 在 git 历史、曾内置于已分发 ReportEditor 0.3.x 安装包，演示库有公网 cpolar 隧道——需现场改库口令并更新 `SD_SMA_DB_PASSWORD` 注入。
- **git 历史清洗**：BFG / `git filter-repo` 清历史口令与误提交大 blob，需 force push，须先协调所有协作者与 Gitea 分支；本机不执行，脚本待确认后提供。
- **小尾巴**：web_config `GET /api/config/file` 仍回传 password 字段（现被鉴权挡住，建议响应脱敏）；`tests/test_time.py`/`test_datatype.py` 是手工脚本，全目录 `pytest` 收集会报错（按文件跑不受影响）。
- 细节见 001 对应 H1。

---

## 后续登记（2026-07-13 起）

- **数据源滑动解锁 UI + 60s 自动上锁（⌛️ 仅登记）**：[docs/017-⌛️-ReportEditor数据源滑动解锁与限时上锁.md](017-⌛️-ReportEditor数据源滑动解锁与限时上锁.md)
- **AI 对话 UI 改版（⌛️ 仅登记）**：[docs/016-⌛️-ReportEditor-AI对话UI改版.md](016-⌛️-ReportEditor-AI对话UI改版.md)
