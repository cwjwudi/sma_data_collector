# 003 · 剩余任务与后续规划（跨 001/002 汇总）

> **快照日期**：2026-07-12（索引正文仍以当日为准；其后新增看板见文末「后续登记」）。本文档是「未完成任务 + 后续规划」的**统一索引/路线图**，深细节引用
> [`001-✅-安全与可靠性缺陷修复.md`](001-✅-安全与可靠性缺陷修复.md)（A 档已关）、
> [`002-🚧-表格系统评估与修复.md`](002-🚧-表格系统评估与修复.md)、
> [`028-⌛️-口令轮换与git历史清洗.md`](028-⌛️-口令轮换与git历史清洗.md)（原 001 C 档），此处不复制正文。
> 每个 H1 是一条可执行/可验收的剩余子任务，按 CLAUDE.md 约定标 ⌛️/🚧。

---

## 当前进度快照（git）

- 索引以看板状态为准；版本线已至 **0.3.109**（029 缩略图懒加载）。
- **仍需拍板**：盲区能力；**现场**：见 [028](028-⌛️-口令轮换与git历史清洗.md)。

## 分类速查（挑任务用）

| 分类 | 含义 | 涉及任务 |
| ---- | ---- | ---- |
| 🟢 本机可纯逻辑闭环（TDD） | 代码+单测在本机即可验收 | （表格债多数已落地）当前剩余多为需跑应用/决策项 |
| 🖥 需运行应用核对 | 要 `npm run electron:dev` 看视觉/手感 | （可选）0.3.108 行内断行 / 027 手测 |
| 🧭 需产品决策 | 改动会变可见行为或涉及需求取舍 | 盲区能力（合并单元格等） |
| 🏭 需现场 / 真库 / 协调 | 依赖真实环境或多人协调 | [028](028-⌛️-口令轮换与git历史清洗.md) 口令轮换/历史清洗；（可选）P2-A 真 MySQL 点验 |

## 建议推进顺序

1. **（可选手测）0.3.108** 窄列超高文本导出预览多页续排无裁字。
2. **盲区能力** 做不做、做哪些（🧭）。
3. **现场窗口**：见 [028](028-⌛️-口令轮换与git历史清洗.md)——口令轮换（高优先）+ git 历史清洗。

---

# ✅ 已完成：历史报表缩略图懒加载不触发（0.3.109）

- 看板：[docs/029-✅](029-✅-ReportEditor历史报表缩略图懒加载不触发.md)。entries 清空后重建 Observer；单测 T1–T7；**Win 手测已通过**（签名库→历史报表自动出图）。
- **导出 PDF 相框 / 表格空间右偏（✅ 0.3.98 · 2026-07-20 复核）**：[docs/019-✅](019-✅-ReportEditor导出PDF纸张外框.md) — 根因 slack+inset 欠缩放；U1–U9 仍绿；**0.3.109 未打安装包**（SHA 空）
- **结批占满 CPU → mappView 白屏（🚧 · 2026-07-20 登记）**：[docs/030-🚧](030-🚧-ReportEditor结批占满CPU导致mappView白屏.md) — Electron 导出不节流+默认并行4；现场先并行=1

# ✅ 已完成：P1-B 续 · 单行超页真·行内跨页断行（0.3.108）

- 导出预览按视觉行拆分同一逻辑行跨页；静态表 + SQL 填充。细节见 002。建议手测窄列长文本。

# ✅ 已完成：历史报表拷移 / U 盘记入操作审计（0.3.107）

- 看板：[docs/027-✅](027-✅-ReportEditor历史报表拷移与U盘审计.md)。确认打开/忽略、手选右侧、左⇄右复制移动写审计。

# ✅ 已完成：P1-A 预览截断提示 UI 横幅接入（0.3.105）

- 属性面板 `TemplateTableSqlFillFields` 顶部显示截断提示（正文 `tblfill:` / 版式 `ztblfill:`）。细节见 002。

# ✅ 已完成：P2-A SQL 参数值真参数化下发（0.3.104）

- 前端 `bindScalarSqlParams` + 预览管线（标量/表格填充）下发 `params`；后端 `DbExecuteSqlRequest.params` → `run_*_readonly`。
- 单测：`scalar-sql-params.test.ts`、`test_query_sql_params.py`。细节见 002。
- （可选 🏭）真 MySQL 反斜杠参数点验。

# ✅ 已完成：P2-C 续 · 对齐 intrinsicOuterHeight SQL 填充高度漂移（0.3.106）

- **决策**：以正文为准（内容感知行高）；去掉 Zone SQL `chrome+rows*minH` 短路。
- 单测：`table-metrics-readonly.test.ts`（长文案抬高外框）。细节见 002。

# ✅ 已完成：P3-B 拖拽 deep-watch 防抖 + 高缩放亚像素余数（0.3.105）

- 列目录 deep watch 200ms 防抖；`takeLayoutDeltaFromScreen` + 单测。建议 2.8× 手感点验。细节见 002。

# ⌛️ 未完成：盲区类能力缺口（需求确认后再定，非 bug）

- 合并单元格（rowspan/colspan）、行列只能末尾增删（无法删中间列/重排）、拆分导出无统一数据快照/无 AbortController、超页大行无虚拟化、静态表跨页不重复表头、时区不换算、无千分位、拖拽把手键盘不可达。
- 均需**需求确认**优先级后再排（🧭）。清单见 002 对应 H1。

# ⌛️ 未完成：口令轮换与 git 历史清洗（原 001 C 档 → 028）

- **看板**：[`028-⌛️-口令轮换与git历史清洗.md`](028-⌛️-口令轮换与git历史清洗.md)
- **口令轮换（高优先 🏭）**：现场改库口令 + `SD_SMA_DB_PASSWORD`；收紧演示库公网入口；旧安装包视为已泄密。
- **git 历史清洗**：需协调 + force push；Agent 默认不执行；说「出清洗手册」后再给步骤。
- **小尾巴**（可选）：web_config 响应脱敏；手工 pytest 脚本收集卫生——见 028。

---

## 后续登记（2026-07-13 起）

- **数据源滑动解锁 UI + 60s 自动上锁（✅ 0.3.93）**：[docs/017-✅-ReportEditor数据源滑动解锁与限时上锁.md](017-✅-ReportEditor数据源滑动解锁与限时上锁.md)
- **AI 对话 UI 改版（✅ 0.3.94 已合入）**：[docs/016-✅-ReportEditor-AI对话UI改版.md](016-✅-ReportEditor-AI对话UI改版.md)
- **探活 claim 误伤只读（✅ 0.3.95）**：[docs/006-🚧-ReportEditor-AI上游错误体验.md](006-🚧-ReportEditor-AI上游错误体验.md)
- **普通表格单元格可视化 SQL 保存失败（✅ 0.3.101）**：[docs/024-✅-ReportEditor普通表格单元格可视化SQL保存失败.md](024-✅-ReportEditor普通表格单元格可视化SQL保存失败.md)
- **Windows 插 U 盘无分屏右侧提示（✅ 0.3.102）**：[docs/025-✅-ReportEditor-Windows插U盘无分屏提示.md](025-✅-ReportEditor-Windows插U盘无分屏提示.md) — Removable+USB+新盘符差集
- **P2-A SQL 真参数化（✅ 0.3.104）**：[docs/002-🚧-表格系统评估与修复.md](002-🚧-表格系统评估与修复.md)
- **AI 悬浮按钮可拖动（✅ 0.3.103）**：[docs/026-✅-ReportEditor-AI悬浮按钮可拖动.md](026-✅-ReportEditor-AI悬浮按钮可拖动.md)
- **历史报表拷移 / U 盘记入审计（✅ 0.3.107）**：[docs/027-✅-ReportEditor历史报表拷移与U盘审计.md](027-✅-ReportEditor历史报表拷移与U盘审计.md) — 确认打开可移动卷 + 复制/移动写操作审计
- **历史报表缩略图懒加载不触发（✅ 0.3.109）**：[docs/029-✅-ReportEditor历史报表缩略图懒加载不触发.md](029-✅-ReportEditor历史报表缩略图懒加载不触发.md) — keep-alive 重回后重建 Observer
- **口令轮换与 git 历史清洗（⌛️ · 原 001 C 档）**：[docs/028-⌛️-口令轮换与git历史清洗.md](028-⌛️-口令轮换与git历史清洗.md)
- **001 A 档安全修复（✅ 已关单）**：[docs/001-✅-安全与可靠性缺陷修复.md](001-✅-安全与可靠性缺陷修复.md)
