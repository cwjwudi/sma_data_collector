# 🚧 进行中：PLC 生产数据仿真改造

目标是让 `SD_SMA_SCADA_DEMO` 持续生成与配方、产品、报警、审计和批次之间具有业务关联的数据。

验收证据包括 AS `cp1586` 构建日志、RUC 下载安全检查、PVI 读回和 PLC Logger。

# ⏳️ 未完成：全数据组采集与批次流程验证

使用 `config/collector/AA_SMA_DATA_TEST.json`，覆盖 6 个数据组。`Data_Product` 同时验证 5 秒时间采集和变量触发采集。

至少完成 3 个完整开批、生产、结批流程，核对 BatchCode、时间边界、单调计数和 batch upsert 约束。

# ⏳️ 未完成：一小时压力测试与 PVI 随机触发

正式计时不少于 60 分钟。期间通过 PVI 随机写入各任务的手动触发入口，保存随机种子、写入时间、读回、PLC 复位和数据库结果。

# ⏳️ 未完成：意外中断与恢复测试

覆盖采集器进程强制终止后重启、PLC 软件重启与恢复后采集。核对 SQLite 持久化 outbox、MySQL 实际提交、触发位复位以及批次状态。

# ⏳️ 未完成：数据库索引与最终报告

对实际表和新分区表查询 `information_schema.statistics`，核对配置的 BatchCode BTREE 索引，并使用 `EXPLAIN` 验证典型查询确实使用索引。
