# ReportEditor 数据源锁定后连接表单按钮被顶出

> 本文件为 **任务看板**；规则见 [CLAUDE.md](../CLAUDE.md)。  
> 相关组件：`ConnectionManager.vue`、`connection-form-pane.css`；数据源滑动锁见 `DatasourceLockToggle.vue`。  
> 相近历史：[docs/004-✅-ReportEditor数据源UI修复.md](004-✅-ReportEditor数据源UI修复.md)。

---

# ⌛️ 未完成：锁定提示挤占高度，底部操作按钮被顶出可视区

## 现象（现场截图 · 2026-07-13）

- 数据源 → 数据库工作台 → 连接 `SMA_DATABASE`。  
- **数据源已锁定**时，表单顶部多出一条蓝底提示：「数据源已锁定，仅可查看与测试连接。」  
- 相对未锁定状态，表单内容整体下移；底部 **「删除」** 等操作按钮被挤到面板白底之外 / 视口下方，需额外滚动才能看到，观感像布局破损。  
- 右侧架构预览正常；问题仅在左侧 `conn-form-pane`。

## 根因（代码对照 · 文档阶段）

`ConnectionManager.vue` 在锁定时于表单顶部插入：

```vue
<p v-if="locked" class="demo-conn-hint">数据源已锁定，仅可查看与测试连接。</p>
```

`.conn-form-pane` / `.actions`（见 `connection-form-pane.css`）为常规文档流排布，**未**为操作区做吸底或为可滚动区预留 hint 高度；多出 hint 后，固定高度侧栏内底部按钮被推出可视区。

同类：远程演示连接也有 `.demo-conn-hint`，长提示时可能同类风险。

## 拟改（确认后开工）

1. 侧栏表单：内容区 `overflow-y: auto`，操作按钮 **吸底固定**（或 sticky），hint 不挤掉按钮。  
2. 或缩短锁定文案 / 并入标题行，减少占位。  
3. 验收：锁定 / 未锁定 / 远程演示三种状态下，「测试连接 / 仅保存 / 测试并保存 / 删除」均完整可见，无需猜滚动。

## 测试用例（实现时必须覆盖；先红后绿）

> 约定：左侧面板指 `.conn-form-pane` 或其滚动父级在**默认工作台视口高度**下的可见区域；「完整可见」= 按钮四个角均在该可视矩形内（或操作条固定在面板底且始终露出）。

### A. 组件行为（vitest + `@vue/test-utils` · 建议扩 `ConnectionManager.test.ts`）

| # | 用例 | 前置 | 操作 / 断言 |
|---|------|------|-------------|
| A1 | 锁定时展示提示 | `modelValue` 为已保存连接，`locked: true` | 文案含「数据源已锁定」；存在 `.demo-conn-hint` |
| A2 | 未锁定无锁定提示 | 同上，`locked: false` | 无「数据源已锁定」hint（演示连接除外） |
| A3 | 锁定时操作区仍在 DOM | 已保存连接 + `locked: true` | `.actions` 存在；含「测试连接」「仅保存」「测试并保存」「删除」 |
| A4 | 锁定时字段只读 | 同上 | 名称等 `input`/`select` 为 `disabled`；「测试连接」仍可点（`:disabled` 不因 locked 单独禁用测试按钮——与现逻辑一致：`busy` 才禁测试） |
| A5 | 锁定 ↔ 未锁定切换 | `setProps({ locked })` 来回切 | hint 显隐正确；`.actions` 始终存在，不因切换卸载 |
| A6 | 远程演示 + 锁定 | 远程演示连接形态（若可构造 `isRemoteDemo`） | 演示说明与锁定提示不互相拆掉 `.actions` |

### B. 布局 CSS 契约（vitest · 建议扩 `workbench-layout.test.ts` 或新建 `connection-form-pane-layout.test.ts`）

| # | 用例 | 断言（修后应满足；修前可先红） |
|---|------|--------------------------------|
| B1 | 面板为可约束高度的列布局 | `.conn-form-pane` 为 `flex-direction: column`（已有则可保留） |
| B2 | 操作条不随内容滚出 | `.actions` 有 `flex-shrink: 0`，且为 sticky 底 / 面板底固定之一；**或** 内容区有独立 `overflow-y: auto` 而 `.actions` 在滚动区外 |
| B3 | 禁止仅靠文档流堆 hint 顶掉按钮 | 契约：存在「可滚动主体 + 固定操作区」结构类名（实现时定名，如 `.conn-form-pane__body` / `__actions`），测试读 CSS/模板字符串锁定 |
| B4 | 与 004 不回归 | 仍满足：`.conn-form-pane` **不** `height: 100%`（见既有 `workbench-layout.test.ts`） |

### C. 几何可见性（可选 · happy-dom / 固定高度挂载）

| # | 用例 | 做法 |
|---|------|------|
| C1 | 矮视口 + 锁定 | 给面板父级设固定高度（如 360px），`locked: true` + 完整表单字段；断言 `.actions` 的 `getBoundingClientRect()` 底边 ≤ 面板可视底边 |
| C2 | 未锁定同高度 | 同 C1 但 `locked: false`；`.actions` 同样完整可见（基线） |
| C3 | 锁定比未锁定多 hint | 两者 `.actions` 底边均不溢出；允许内容区出现纵向滚动条 |

> C 组若 happy-dom 几何不稳定，允许用 B 组契约代替，但手工 D 组不可省。

### D. 手工验收（发版 / 真机）

| # | 步骤 | 期望 |
|---|------|------|
| D1 | 数据源解锁 → 选中任意已保存连接 → 窗口高度调到约半屏 | 「测试连接 / 仅保存 / 测试并保存 / 删除」一眼可见或仅内容区滚动、按钮条仍在 |
| D2 | 滑动锁定同一连接 | 出现锁定提示后，上述按钮**仍完整可见**（不必滚出白底找「删除」） |
| D3 | 锁定状态下点「测试连接」 | 仍可测试；保存类按钮 disabled 符合锁定只读 |
| D4 | 再解锁 | 提示消失；按钮布局不跳动到面板外 |
| D5 | （若有）远程演示连接 + 锁定 | 同 D2 |

### E. 回归边界

| # | 场景 | 期望 |
|---|------|------|
| E1 | 空列表 / 仅「+ 新建」 | 不因本改动空白或丢 pane（004 回归） |
| E2 | OPC UA 左侧同用 `conn-form-pane` 的表单 | 长提示或锁定类文案时操作区同样不被顶出（若共用 CSS，一条契约覆盖） |
| E3 | 窄宽度侧栏 | 按钮可换行，但不被裁切出面板 |

## 本轮范围

- ✅ 记录现象与定位要点（本文档）  
- ✅ 补充测试用例矩阵（A–E）  
- ⌛️ 布局修复与按矩阵落地单测（待开工）
