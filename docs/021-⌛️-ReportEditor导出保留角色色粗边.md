# ReportEditor 导出 PDF：保留封面橙 / 正文蓝紫粗色边

> 本文件为 **任务看板**；规则见 [CLAUDE.md](../CLAUDE.md)。  
> **流程**：政策变更已登记；**未开工改代码**。  
> **澄清**：2026-07-14 · 用户明确「橙色和蓝紫色粗色边我想要保留」。  
> **合批（2026-07-14）**：与 [docs/019](019-⌛️-ReportEditor导出PDF纸张外框.md) **同一版本一起实施**；详细用例以 019「合批测试用例」为准，本条只列角色色相关子集。  
> **与 009 关系**：[docs/009-✅](009-✅-ReportEditor导出纸张橙边框.md) 曾于 **0.3.67** 剥离导出角色色；本条为**政策反转**，不在 009 上续写验收勾。

---

# ⌛️ 未完成：导出成品保留角色色粗边（与 019 合批）

## 产品诉求

- **保留**（恢复到 PDF 成品）封面 **橙色**、正文 **蓝紫 / 靛蓝**、末页 **紫** 粗色边。  
- 模版管理缩略图装饰色——保持。  
- 编辑器正文画布——不加该 chrome。

## 为何必须与 019 一起做

| 若只做… | 风险 |
|---------|------|
| 只恢复角色色（本条） | 内容仍欠缩放 → **白衬 + 色边**叠成更明显的「双框」 |
| 只修铺满（019） | PDF 仍无橙/蓝紫边，不符合「要保留粗色边」 |
| **合批** | 先 1:1 铺满，再贴纸缘角色色 → 单一发版验收 |

## 现状

| 表面 | 角色色粗边 |
|------|------------|
| 模版列表缩略图 | ✅ 有（`plain=false`） |
| 导出 PDF（0.3.67+） | ❌ 无（`plainChrome` + 打印 CSS 双清） |
| 编辑画布 | 无 |

## 拟拍板（默认 ★）

| # | 问题 | 默认 ★ |
|---|------|--------|
| **Q1** | 恢复范围 | 导出 PDF：封面橙 / 正文靛蓝 / 末页紫 |
| **Q2** | 实现方式 | `omitCaptions` 与 `plain` **解耦**；导出 `plainChrome=false`；打印 CSS 停止清角色边 |
| **Q3** | 与 019 | **同版本合批**；实施顺序见 019 |
| **Q4** | 旧单测 | 改写 `pdf-export-plain-chrome.test.ts`（009「必须去色」断言作废 → 021/019 新契约） |

## 实现范围（开工后 · 合批内本条部分）

1. `TemplateExportPreviewStack`：omit ≠ plain。  
2. `PdfExportView`：打印样式保留 `.mpc` 角色 outline / 纸边色。  
3. 与 019 同 PR / 同 bump；收口后两看板同标 ✅。

## 验收（角色色子集；完整表见 019）

- [ ] V1–V3：封面橙 / 正文蓝紫 / 末页紫可见且铺满  
- [ ] V5–V6：列表有色、画布无装饰框  
- [ ] U5–U8：omit/plain 解耦与打印 CSS 契约  

## 合批测试用例（本条相关 · 全文在 019）

| ID | 要点 |
|----|------|
| **U5–U8** | omit ≠ plain；打印不清角色边；plain API 仍保留给非导出 |
| **V1–V3** | 三页种角色色目视 |
| **V5–V6** | 列表 / 画布回归 |
| **V8** | 相对 0.3.67+：角色边从无到有 |
| **N3** | `showBorder` 与角色粗边分离 |

完整 U/V/N 表 → [019 · 合批测试用例](019-⌛️-ReportEditor导出PDF纸张外框.md#合批测试用例019--021--开工时落地)。

## 不做（本条登记）

- 本轮不改代码（与 019 一同等「开工」）  
- 不删 009 历史记录  
- 不单独发「只恢复色边、不修白衬」的版本  
