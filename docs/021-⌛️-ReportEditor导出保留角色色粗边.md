# ReportEditor 导出 PDF：保留封面橙 / 正文蓝紫粗色边

> 本文件为 **任务看板**；规则见 [CLAUDE.md](../CLAUDE.md)。  
> **流程**：政策变更已登记；**未开工改代码**。  
> **澄清**：2026-07-14 · 用户明确「橙色和蓝紫色粗色边我想要保留」。  
> **与 009 关系**：[docs/009-✅](009-✅-ReportEditor导出纸张橙边框.md) 曾于 **0.3.67** 从导出 PDF **剥离** MiniPreviewChrome 角色色纸边；本条为**政策反转**（要保留/恢复），不在 009 上续写验收勾。  
> **与 019 关系**：[docs/019](019-⌛️-ReportEditor导出PDF纸张外框.md) 只管**白衬边 / 欠缩放**；**不得**再把「去掉角色色」当作 019 目标。

---

# ⌛️ 未完成：导出成品保留角色色粗边

## 产品诉求

- **保留**（或恢复到 PDF 成品中）封面 **橙色**、正文 **蓝紫 / 靛蓝** 等角色色**粗色边**（末页紫边默认一并保留，与 0.3.67 前一致，除非用户另说）。  
- 模版管理缩略图本来就有装饰色——保持。  
- 编辑器正文画布本来没有该 chrome——保持（不往画布加橙框）。

## 现状

| 表面 | 角色色粗边 |
|------|------------|
| 模版列表缩略图 | ✅ 有（`plain=false`） |
| 导出 PDF（0.3.67+） | ❌ 无（`plainChrome` + 打印 CSS 双清） |
| 编辑画布 | 无（不走 MiniPreviewChrome） |

## 拟拍板（默认 ★）

| # | 问题 | 默认 ★ |
|---|------|--------|
| **Q1** | 恢复范围 | **导出 PDF** 重新带上封面橙 / 正文靛蓝 / 末页紫（与 0.3.67 前视觉一致） |
| **Q2** | 实现方式 | 导出路径 **关闭 plain**：`pdfExportOmitCaptions` 时仍省略标题文字，但 **`plainChrome=false`**（或拆成两个 prop）；并**去掉** `PdfExportView` 打印 CSS 里对 `.mpc` outline / 角色色纸边的清除（可保留清 `tep-card` padding / 灰边若与角色色无关） |
| **Q3** | 与铺满纸面 | 角色色边占在纸缘外或纸内需复测：若边在 `@page` 内会吃内容区，优先 **边画在纸盒上且 scale=1 后仍整页对齐**；与 019 欠缩放修复**同开或先 019 再本条**，避免边 + 白衬叠出双框 |
| **Q4** | 单测 | 更新 / 替换 `pdf-export-plain-chrome.test.ts`：导出不再要求 `mpc--plain` 清角色色；改为断言导出仍传 omitCaptions、且**保留**角色边相关 class/CSS |

## 实现范围（开工后）

1. `TemplateExportPreviewStack`：omit captions ≠ 强制 plain。  
2. `PdfExportView` 打印样式：停止抹掉 `.mpc` outline / `.mpp-paper` 角色色边（细调：灰 `#d4d4d8` 与阴影是否保留随视觉验收）。  
3. 与 [019](019-⌛️-ReportEditor导出PDF纸张外框.md) 协调：内容 1:1 铺满后，角色粗边应贴纸缘而非「缩进一圈白边再套色边」。  
4. 文档：009 顶栏加「政策已由 021 反转」指针；本条收口后标 ✅ + 版本号。

## 验收

- [ ] 导出 PDF：封面可见橙色粗边、正文可见蓝紫粗边、末页可见紫边（与列表缩略语义一致）  
- [ ] 编辑器画布仍无该装饰框  
- [ ] 列表缩略图角色色不回归丢失  
- [ ] 无系统性白衬边（若 019 已做）或本条注明依赖 019  

## 不做（本条登记）

- 本轮不改代码  
- 不删 009 历史记录  
- 不把「去角色色」写回 019 验收  
