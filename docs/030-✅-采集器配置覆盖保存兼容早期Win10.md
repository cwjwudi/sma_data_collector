# ✅ 已完成：修复早期 Win10 覆盖保存配置失败

目标：解决 Win10 1503/1703 等早期版本上，覆盖保存已有采集器配置时出现 `[WinError 127] The specified procedure could not be found`。

实现范围：

- 备份配置时只复制 JSON 文件内容，不再复制文件时间和权限元数据。
- 保留原有 `_backup` 备份目录和临时文件原子替换流程。
- 增加覆盖保存回归测试，验证旧内容被备份且新内容成功写入。

验收证据：

- `tests/test_config_backup.py` 使用模拟的 WinError 127 防护，确认覆盖保存不再调用 `copy2()`。
- 备份文件保留旧 JSON 内容，目标文件写入新 JSON 内容。
- 采集器完整测试集：`170 passed`。
- 仅有 4 条已有 pytest/Starlette 警告，本次未新增测试失败。
