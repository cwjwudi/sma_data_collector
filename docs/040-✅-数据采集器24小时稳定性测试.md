# ✅ 已完成：AA_test 24 小时稳定性测试

测试对象：

- 采集器：`_Prj/SD_SMA_DATA_COLLECTOR`
- 配置：`config/collector/AA_test.json`
- 专用测试 PLC：`192.168.50.233`（X20CP1586，AR J4.93）
- OPC UA：`opc.tcp://192.168.50.233:4840`
- MySQL：`192.168.50.22:3306/sma_data_test`
- 数据组：`Data_Recipe`、`Data_Product`、`Data_Alarm`、`Data_Audit`、`Data_Batch`、`Data_BatchInfo`

验收窗口为连续 24 小时（86400 秒）。采集器必须保持运行，六个数据组均持续产生采集事件和数据库新增记录；运行结束前连接恢复为 `connected`，内存存储队列无残留，数据库提交无失败。

正式窗口于 `2026-07-30 11:21:25 +08:00` 启动，进程为 `uv.exe` PID 7408、`python.exe` PID 3424，计划于 `2026-07-31 11:21:25 +08:00` 后结束。受控 OPC UA 断线安排在窗口的 6、12、18 小时。

可复现性基线：

- Git 分支 `test/aa-full-function-regression`，启动时基线提交 `a6942e8ca800af0cee38899ea6ab3e56ee7f514f`；工作区另有与本测试无关的既存修改，最终提交时排除。
- `AA_test.json` 的 SHA-256 为 `453B061DB57EC07CB6F1A297AFB6B8504B56D3E376F87F4037EBE7CCF0D5218F`。
- 运行环境：uv 0.11.23、Python 3.12.13、asyncua 2.0.1、PyMySQL 1.4.6。
- 实际启动参数：`tools/aa_full_function_soak.py <AA_test.json> --duration 86400 --disconnect-at 21600 --disconnect-at 43200 --disconnect-at 64800 --report <stability_24h.json>`。

测试证据写入：

- `test_results/aa_test_24h/collector_stdout.log`
- `test_results/aa_test_24h/collector_stderr.log`
- `test_results/aa_test_24h/process_resources.csv`
- `test_results/aa_test_24h/process_resources_final.csv`
- `test_results/aa_test_24h/database_checkpoints.jsonl`
- `test_results/aa_test_24h/pvi_disturbances.jsonl`
- `test_results/aa_test_24h/incidents.jsonl`
- `test_results/aa_test_24h/run_manifest.json`
- `test_results/aa_test_24h/acceptance_checklist.json`
- `test_results/aa_test_24h/stability_24h.json`
- 本文的 PVI 扰动时间线与最终结论

# ✅ 已完成：PVI 外部扰动与触发插入验证

使用 MCP PVI 工具连接专用测试 PLC，并通过 PLC 已有的手动入口触发业务变量，避免直接制造不一致的业务快照：

- `DataSQLRec:bManualTrigger` → `gDataSQLOperate.RecipeInsert`
- `DataSQLPro:bManualTrigger` → `gDataSQLOperate.ProductInsert`
- `DataSQLAla:uiManualIndex` + `DataSQLAla:bManualTrigger` → `AlarmInsertBuffer.InsertTrigger[index]`
- `DataSQLAud:uiManualIndex` + `DataSQLAud:bManualTrigger` → `AuditInsertBuffer.InsertTrigger[index]`
- `DataSQLBat:uiManualCommand=1/2` → 开批/结批，覆盖 `BatchInsert` 与批次信息触发

每轮记录 PVI 写入前后的数据库行数、PLC 触发位复位结果、采集器事件增量及插入延迟。扰动索引和时间点在执行时追加，测试结束后确认手动入口与业务触发位均恢复。

## 第 1 轮：启动基线与人工触发

- `2026-07-30 11:21:27 +08:00`：MySQL 初始化成功，OPC UA subscription 建立。
- 60 秒进度：六组事件均大于零；`db_rows_committed=267`，连接为 `connected`，错误、pending、retry、dead-letter 均为 0。
- `2026-07-30 11:23:29 +08:00`：MCP PVI 写入 7/7 成功。Alarm 使用索引 73，Audit 使用索引 74；Recipe、Product、Alarm、Audit 的手动触发置位；检测到批次已打开，因此写入 `uiManualCommand=2` 触发结批。
- PVI 审计：`br_device_autodev/var/audit/2026-07-30/032329_308544_plc_write_pvi_started_e670c513.json`、`032331_996273_plc_write_pvi_succeeded_00a1b1db.json`。
- 写后读回 12/12 成功：四个 `bManualTrigger=false`、`uiManualCommand=0`，Recipe/Product/Batch/BatchInfo 业务触发均为 `false`，Alarm/Audit 各 100 位触发数组残留 `true=0`。
- 查库窗口 `11:23:11` → `11:23:52`：Recipe `+2`、Product `+46`、Alarm `+4`、Audit `+2`、Batch `+1`、BatchInfo `+3`。由于 PLC 仿真自动触发仍并行运行，该增量包含自动事件；PVI 置位被 PLC 接受、采集器事件增长且所有触发位由采集器复位，证明外部扰动链路已闭环。

## 启动后 5 分钟速率基线

`elapsed=300.4s` 时累计收到 Product 308、Alarm 141、Audit 131、Recipe 10、Batch 7、BatchInfo 9 个事件；数据库提交 584 行，平均约 `116.6 行/分钟`。连续五个一分钟采样中连接均为 `connected`，`errors=0`，pending/retry/dead-letter 均为 0。该基线用于与 6/12/18 小时断线及 PVI 扰动后的速率对照。

## 第 2 轮：稳定运行期人工触发

- `2026-07-30 11:28:17 +08:00`：MCP PVI 写入 7/7 成功，使用不同的 Alarm 索引 83、Audit 索引 84；四个手动触发置位，并在批次已打开时执行 `uiManualCommand=2`。
- PVI 审计：`br_device_autodev/var/audit/2026-07-30/032817_556676_plc_write_pvi_started_8b77b57c.json`、`032820_235863_plc_write_pvi_succeeded_2d346121.json`。
- `11:28:03` → `11:28:40` 查库增量：Recipe `+5`、Product `+40`、Alarm `+11`、Audit `+9`、Batch `+1`、BatchInfo `+1`。
- 写后第一次读回时 BatchInfo 业务触发仍为 `true`，说明它正处于本次新批次的消费窗口；数秒后再次读回变为 `false`。最终四个 `bManualTrigger=false`、`uiManualCommand=0`，Alarm/Audit 触发数组无残留，所有业务触发均已消费。
- 同期 `elapsed=420.1s`：六组事件继续增长，`db_rows_committed=759`，连接为 `connected`，错误及积压均为 0。

## 15 分钟运行检查点

`elapsed=900.0s`：Product 925、Alarm 225、Audit 200、Recipe 30、Batch 21、BatchInfo 42；数据库累计提交 1440 行。连接保持 `connected`，`errors=0`，pending/retry/dead-letter 均为 0。

## 30 分钟运行检查点

`elapsed=1800.3s`：Product 1847、Alarm 359、Audit 294、Recipe 55、Batch 39、BatchInfo 90；数据库累计提交 2670 行。相对 15 分钟检查点新增 1230 行，平均约 `82.0 行/分钟`；连接保持 `connected`，`errors=0`，pending/retry/dead-letter 均为 0。

## 45 分钟运行检查点

`elapsed=2700.2s`：Product 2769、Alarm 489、Audit 399、Recipe 80、Batch 58、BatchInfo 126；数据库累计提交 3920 行。相对 30 分钟检查点新增 1250 行，平均约 `83.3 行/分钟`；连接保持 `connected`，`errors=0`，pending/retry/dead-letter 均为 0。

## 1 小时运行检查点

`elapsed=3600.5s`：Product 3692、Alarm 629、Audit 486、Recipe 105、Batch 77、BatchInfo 166；数据库累计提交 5138 行。相对 45 分钟检查点新增 1218 行，平均约 `81.2 行/分钟`；连接保持 `connected`，`errors=0`，pending/retry/dead-letter 均为 0。

采集器运行进程 PID 3424：累计 CPU 21.53 秒、工作集 73.5 MiB、6 个线程；启动后工作集未见持续增长，`collector_stderr.log` 仍为 0 字节。

## 90 分钟运行检查点

`elapsed=5400.3s`：Product 5533、Alarm 895、Audit 671、Recipe 156、Batch 115、BatchInfo 255；数据库累计提交 7619 行。相对 1 小时检查点新增 2481 行，平均约 `82.7 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。检查时采集器 PID 3424 累计 CPU 29.72 秒、工作集 36.5 MiB，`collector_stderr.log` 仍为 0 字节。

## 2 小时运行检查点

`elapsed=7200.1s`：Product 7374、Alarm 1163、Audit 876、Recipe 207、Batch 153、BatchInfo 317；数据库累计提交 10088 行。相对 90 分钟检查点新增 2469 行，平均约 `82.3 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。检查时采集器 PID 3424 累计 CPU 37.81 秒、工作集 34.0 MiB、14 个线程，`collector_stderr.log` 仍为 0 字节。

独立数据库快照（`2026-07-30 13:22:35 +08:00`）交叉核验：Recipe 7061、Product 52496、Alarm 80943、Audit 77015、Batch 1199、BatchInfo 7772 行。六张表的 `max(collection_time)` 均位于本次运行窗口，范围为 `13:20:35` 至 `13:22:34`，确认不是仅有采集器内部计数增长，数据库端确有持续写入。

`2026-07-30 13:33:05 +08:00` 启动旁路资源采样器（PID 33276），每 5 分钟把采集器 PID 3424 的 CPU、工作集、线程数和句柄数追加至 `test_results/aa_test_24h/process_resources.csv`。首条记录：工作集 35835904 字节、15 个线程、305 个句柄；采样器只调用操作系统只读进程查询，不访问 PLC、数据库或采集业务。

第二个采样点（`13:38:05`）为工作集 35844096 字节、15 个线程、305 个句柄；5 分钟内工作集仅变化 8192 字节，线程与句柄完全不变，初步确认资源已进入稳定平台。

按本次采集器 PID 3424 审计 `logs/data_collector.log`：未发现 `ERROR`、`Exception`、`Traceback` 或 `FATAL`；只有启动时 `11:21:27` 的一条 asyncua `WARNING`，内容为 PLC 对请求的 subscription 参数返回修订值。该参数协商后连接成功且 warning 未重复，不计为运行故障。全日志目录中的其他告警来自 7 月 13、27、29 日历史测试，已排除。

## 2.5 小时运行检查点

`elapsed=9000.3s`：Product 9223、Alarm 1440、Audit 1055、Recipe 261、Batch 191、BatchInfo 430；数据库累计提交 12597 行。相对 2 小时检查点新增 2509 行，平均约 `83.6 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。

资源采样器在 `13:43:05` 观察到 asyncio 默认工作线程池由 15 扩至 16，句柄由 305 变为 309；随后 `13:48:05` 的下一采样点仍为 16 线程、309 句柄，未继续增长。2.5 小时检查时工作集 35926016 字节（约 34.3 MiB），相对 `13:33:05` 首点仅增加约 88 KiB，当前未见资源泄漏趋势。

本机 `os.cpu_count()=16`，当前 Python 版本的默认 executor 理论上限为 `min(32, cpu_count + 4)=20` 个工作线程。`13:58:05` 资源点显示进程总线程数按需扩至 17、句柄 313，下一次即时复核仍为 17/313，工作集约 34.3 MiB；总线程还包含主线程和通信线程，因此现阶段属于有界线程池惰性扩容，而非无界泄漏。

## 3 小时运行检查点

`elapsed=10800.4s`：Product 11064、Alarm 1705、Audit 1253、Recipe 311、Batch 227、BatchInfo 525；数据库累计提交 15054 行。相对 2.5 小时检查点新增 2457 行，平均约 `81.9 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。

`14:21:59` 独立数据库快照相对 `13:22:35` 的 2 小时快照增量：Recipe +106、Product +3653、Alarm +543、Audit +377、Batch +38、BatchInfo +203，合计新增 4920 行。六张表最新 `collection_time` 均位于当前运行窗口（`14:21:13` 至 `14:22:00`），数据库端持续写入得到独立确认。

3 小时点即时资源值：累计 CPU 54.61 秒、工作集 36151296 字节（约 34.5 MiB）、20 个总线程、323 个句柄。此前 `14:13:05` 与 `14:18:05` 两个正式采样点均为 19 线程/321 句柄，工作集约 34.4 MiB；继续观察线程池达到并发平台后的长期稳定性。

## 3.25 小时运行检查点

`elapsed=11700.2s`：数据库累计提交 16275 行，相对 3 小时点新增 1221 行，平均约 `81.4 行/分钟`。连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。连续资源复核为 21 线程、329 句柄，工作集 36196352 字节（约 34.5 MiB），线程池当前保持平台状态。

## 3.5 小时运行检查点

`elapsed=12600.0s`：Product 12891、Alarm 1972、Audit 1444、Recipe 357、Batch 265、BatchInfo 549；数据库累计提交 17449 行。相对 3 小时检查点新增 2395 行，平均约 `79.8 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。

`14:38:05`、`14:43:05`、`14:48:05` 三个连续资源点均保持 21 线程、329 句柄，工作集仅由 36196352 增至 36208640 字节；检查时工作集 36212736 字节（约 34.5 MiB），资源平台稳定。

## 4 小时运行检查点

`elapsed=14400.1s`：Product 14725、Alarm 2236、Audit 1642、Recipe 409、Batch 303、BatchInfo 575；数据库累计提交 19870 行。相对 3.5 小时检查点新增 2421 行，平均约 `80.7 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。

`15:22:16` 独立数据库快照相对 3 小时快照增量：Recipe +97、Product +3677、Alarm +533、Audit +391、Batch +38、BatchInfo +50，合计新增 4786 行。六张表最新 `collection_time` 均位于当前运行窗口（`15:20:48` 至 `15:22:16`），数据库端持续写入再次得到独立确认。

4 小时点资源值：累计 CPU 71.61 秒、工作集 35524608 字节（约 33.9 MiB）、21 个线程、329 个句柄。最近三个正式资源点保持 21 线程/329 句柄，工作集无上升趋势；`collector_stderr.log` 仍为 0 字节。

## 4.25 小时运行检查点

`elapsed=15300.5s`：数据库累计提交 21093 行，相对 4 小时点新增 1223 行，平均约 `81.5 行/分钟`。连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。资源保持 21 线程、329 句柄，工作集降至 34365440 字节（约 32.8 MiB），无内存增长趋势。

## 4.5 小时运行检查点

`elapsed=16200.2s`：Product 16558、Alarm 2504、Audit 1827、Recipe 455、Batch 341、BatchInfo 616；数据库累计提交 22282 行。相对 4 小时检查点新增 2412 行，平均约 `80.4 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。资源保持 21 线程、329 句柄，工作集约 32.8 MiB。

## 5 小时运行检查点

`elapsed=18000.1s`：Product 18389、Alarm 2776、Audit 2017、Recipe 504、Batch 379、BatchInfo 653；数据库累计提交 24706 行。相对 4 小时检查点新增 4836 行，平均约 `80.6 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。累计 CPU 90.06 秒，资源保持 21 线程、329 句柄，工作集 34541568 字节（约 32.9 MiB）。

## 5.25 小时运行检查点

`elapsed=18900.3s`：数据库累计提交 25898 行，相对 5 小时点新增 1192 行，平均约 `79.5 行/分钟`。连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。最近两个资源点均为 21 线程、329 句柄、34566144 字节工作集（约 33.0 MiB）。

## 5.5 小时运行检查点

`elapsed=19800.2s`：Product 20216、Alarm 3041、Audit 2202、Recipe 552、Batch 417、BatchInfo 689；数据库累计提交 27106 行。相对 5 小时检查点新增 2400 行，平均 `80.0 行/分钟`；连接保持 `connected`，`faults=0`、`errors=0`，pending/retry/dead-letter 均为 0。最近两个资源点均为 21 线程、329 句柄、34570240 字节工作集（约 33.0 MiB）。

## 6 小时运行检查点与首次断线

`elapsed=21600.1s`：Product 22047、Alarm 3292、Audit 2391、Recipe 599、Batch 455、BatchInfo 718；数据库累计提交 29496 行。相对 5 小时检查点新增 4790 行，平均约 `79.8 行/分钟`。`2026-07-30 17:21:25.035 +08:00` 受控断开 OPC UA 传输，`17:21:25.036` 采集器进入重连，`17:21:25.051` 已建立新 subscription（ID 599453078），日志可见的恢复耗时约 15 ms。该分钟状态已经恢复为 `connected`，`faults=1`、`errors=0`，pending/retry/dead-letter 均为 0；后续 `elapsed=21720.2s` 累计提交增至 29648 行，证明恢复后事件与数据库提交持续推进。

断线前后 `collector_stderr.log` 均为 0 字节。`17:23:06` 资源采样为 21 线程、329 句柄、37949440 字节工作集（约 36.2 MiB）；相比断线前短时上升约 3.4 MiB，线程和句柄没有增加。`17:28:06` 下一采样为 37965824 字节、21 线程、329 句柄，5 分钟内仅变化 16 KiB，表明重连后的新平台已稳定但尚未回落；后续断线节点继续比较是否逐次累积。

截至 `17:28:06`，旁路资源记录累计 48 个五分钟样本：工作集范围 34365440–37965824 字节，线程范围 15–21，句柄范围 305–329。线程与句柄在正常的惰性工作池扩容后已长时间停留于上限，首次断线没有突破既有上限。

## 第 3 轮：6 小时断线恢复后的人工触发

- `2026-07-30 17:22:18 +08:00`，MCP PVI 写入 7/7 成功：Alarm 索引 17、Audit 索引 29，Recipe/Product/Alarm/Audit 四个手动触发位置位；写入前批次为打开状态，因此写入 `DataSQLBat:uiManualCommand=2`。
- PVI 成功审计：`br_device_autodev/var/audit/2026-07-30/092221_098807_plc_write_pvi_succeeded_b7022d58.json`。写后 8/8 变量读回成功，四个 `bManualTrigger=false`、`uiManualCommand=0`，Alarm/Audit 索引保持 17/29，说明触发已由 PLC 消费并自动复位。
- 数据库快照 `17:21:56.307` → `17:23:48.106`：Recipe `+4`、Product `+113`、Alarm `+12`、Audit `+19`、Batch `+1`、BatchInfo `+2`，六组均成功新增；Batch 最新采集时间为 `17:22:26`，与本轮命令窗口一致。
- 写后回读时 `bBatchOpened=true`。结合批次表已新增 1 行且命令已自动复位，判断关闭命令已被消费，随后专用测试 PLC 的持续仿真逻辑又自动开启新批次；不额外覆盖仿真状态，保持长稳测试的连续业务流。

## 7 小时 20 分钟运行检查点

`elapsed=26400.1s`：Product 26942、Alarm 3993、Audit 2896、Recipe 729、Batch 555、BatchInfo 834；数据库累计提交 35930 行。相对 6 小时点新增 6434 行，约 `80.4 行/分钟`；连接保持 `connected`，`faults=1`（计划内断线）、`errors=0`，pending/retry/dead-letter 均为 0，stderr 为 0 字节，精确错误检索无命中。

`18:42:22` 独立数据库快照显示六张表最新采集时间均位于 `18:41:16`–`18:42:23`，已追加到 `database_checkpoints.jsonl`。最近资源采样保持 21 线程、329 句柄，工作集从 `18:23` 的 38064128 字节至 `18:38` 的 38178816 字节，15 分钟增加约 112 KiB，首次重连后的平台未出现阶梯式持续增长。

## 8 小时 22 分钟运行检查点

`elapsed=30120.4s`：Product 30725、Alarm 4544、Audit 3296、Recipe 828、Batch 633、BatchInfo 933；数据库累计提交 40935 行。相对 7 小时 20 分钟点新增 5005 行，约 `80.7 行/分钟`；连接保持 `connected`，`faults=1`、`errors=0`，pending/retry/dead-letter、stderr 与精确错误命中数均为 0。

`19:44:24` 独立数据库快照显示六张表最新采集时间均位于 `19:43:28`–`19:44:24`，已形成第 4 条 JSONL 快照。`19:23`–`19:43` 工作集维持 38207488–38244352 字节；线程通常为 21，`19:38` 单个采样短暂为 22、下一采样即回到 21，句柄始终为 329。该瞬时线程未伴随句柄或工作集阶跃，继续观察但当前不构成资源泄漏证据。

## 9 小时 21 分钟运行检查点

`elapsed=33660.2s`：Product 34324、Alarm 5080、Audit 3657、Recipe 919、Batch 707、BatchInfo 1035；数据库累计提交 45694 行。相对 8 小时 22 分钟点新增 4759 行，约 `80.7 行/分钟`；连接保持 `connected`，`faults=1`、`errors=0`，pending/retry/dead-letter、stderr 与精确错误命中数均为 0。

`20:43:12` 第 5 条独立数据库快照显示六张表最新采集时间均位于 `20:42:13`–`20:43:14`。最近两个五分钟资源采样完全一致：38354944 字节工作集、21 线程、329 句柄；首次断线后的资源平台继续稳定。

## 10 小时 26 分钟运行检查点

`elapsed=37560.2s`：Product 38292、Alarm 5661、Audit 4060、Recipe 1017、Batch 789、BatchInfo 1145；数据库累计提交 50942 行。相对 9 小时 21 分钟点新增 5248 行，约 `80.7 行/分钟`；连接保持 `connected`，`faults=1`、`errors=0`，pending/retry/dead-letter、stderr 与精确错误命中数均为 0。

`21:47:27` 第 7 条独立数据库快照显示六张表最新采集时间均位于 `21:45:00`–`21:47:25`。资源采样仍为 21 线程、329 句柄；工作集已回落至 35454976 字节（约 33.8 MiB），较 9 小时点下降约 2.8 MiB，证明首次断线后的约 36.6 MiB 平台并非不可回收的持续增长。

## 11 小时 26 分钟运行检查点与 PVI 预检

`elapsed=41160.1s`：Product 41947、Alarm 6175、Audit 4437、Recipe 1110、Batch 865、BatchInfo 1236；数据库累计提交 55757 行。相对 10 小时 26 分钟点新增 4815 行，约 `80.3 行/分钟`；连接保持 `connected`，`faults=1`、`errors=0`，pending/retry/dead-letter、stderr 与精确错误命中数均为 0。

`22:48:06` 第 8 条独立数据库快照显示六张表最新采集时间均位于 `22:47:27`–`22:48:04`。最近五个资源采样均保持 21 线程、329 句柄，工作集约 35500032–35512320 字节。

12 小时扰动前 PVI 只读预检 8/8 成功：Recipe/Product/Alarm/Audit 的四个手动触发均为 `false`，`uiManualCommand=0`，Alarm/Audit 保留上一轮索引 17/29；批次当前为打开状态。因此下一轮将使用新的 Alarm/Audit 索引 41/53，并在写入前再次确认批次状态后选择开批或结批命令。

## 12 小时运行检查点与第二次断线

`elapsed=43200.2s`：Product 44020、Alarm 6479、Audit 4644、Recipe 1166、Batch 909、BatchInfo 1289；数据库累计提交 58505 行。相对 11 小时 26 分钟点新增 2748 行，约 `80.8 行/分钟`。`2026-07-30 23:21:25.097 +08:00` 执行第二次受控断开，`23:21:25.104` 进入重连，`23:21:25.110` 重建 subscription（ID 599453079），日志可见恢复耗时约 6 ms。该分钟状态已为 `connected`，`faults=2`、`errors=0`，pending/retry/dead-letter 均为 0。

断线前 `23:18:07` 工作集为 35540992 字节；断线后 `23:23:07` 为 37928960 字节，增加约 2.3 MiB。其后连续五个资源采样均保持约 3793–3794 万字节、21 线程、329 句柄，没有继续增长；与首次断线相同，重连形成可回收的短时工作集平台而未增加线程或句柄。

## 第 4 轮：12 小时断线恢复后的人工触发

- 写前 `23:47:57` 独立数据库快照已落盘；PVI 8/8 读取成功，四个手动触发均为 `false`、`uiManualCommand=0`，批次为打开状态。
- `23:48:13`–`23:48:16`，MCP PVI 写入 7/7 成功：Alarm 索引 41、Audit 索引 53，Recipe/Product/Alarm/Audit 四个手动入口置位，并写入 `uiManualCommand=2` 结批。成功审计：`br_device_autodev/var/audit/2026-07-30/154816_093657_plc_write_pvi_succeeded_d2443781.json`。
- 写入工具的即时回读已观察到 Product、Alarm 与 Batch 命令被快速消费；8 秒后的独立 PVI 回读 8/8 成功，四个手动触发全部为 `false`、`uiManualCommand=0`，索引保持 41/53。批次随后被持续仿真逻辑重新打开。
- 数据库快照 `23:47:57.703` → `23:49:08.137`：Recipe `+5`、Product `+70`、Alarm `+16`、Audit `+2`、Batch `+1`、BatchInfo `+2`，六组均成功新增。结构化记录已追加到 `pvi_disturbances.jsonl`；累计 4 轮、28/28 项写入成功，四份成功审计均存在。
- 扰动后 `elapsed=44820.2s` 累计提交增至 60669 行，连接保持 `connected`，`faults=2`、`errors=0`，所有队列为 0。

## 13 小时 28 分钟运行检查点

`elapsed=48480.1s`：Product 49395、Alarm 7248、Audit 5200、Recipe 1304、Batch 1019、BatchInfo 1424；数据库累计提交 65573 行。相对 12 小时点新增 7068 行，约 `80.3 行/分钟`；连接保持 `connected`，`faults=2`、`errors=0`，pending/retry/dead-letter、stderr 与精确错误命中数均为 0。

`2026-07-31 00:49:30` 独立数据库快照显示六张表最新采集时间均位于 `00:48:08`–`00:49:29`，证据文件当前共 11 条快照（包含 PVI 前后窗口）。最近五个资源采样保持 21 线程、329 句柄，工作集 37978112–38002688 字节；第二次断线后的资源平台无继续抬升。

## 14 小时 29 分钟运行检查点

`elapsed=52140.2s`：Product 53128、Alarm 7783、Audit 5577、Recipe 1400、Batch 1095、BatchInfo 1513；数据库累计提交 70470 行。相对 13 小时 28 分钟点新增 4897 行，约 `80.3 行/分钟`；连接保持 `connected`，`faults=2`、`errors=0`，pending/retry/dead-letter、stderr 与精确错误命中数均为 0。

`01:50:31` 第 12 条独立数据库快照显示六张表最新采集时间均位于 `01:48:28`–`01:50:33`。最近五个资源采样完全保持 37961728 字节工作集、21 线程、329 句柄，第二次断线后的平台稳定。

## 15 小时 29 分钟运行检查点

`elapsed=55740.4s`：Product 56785、Alarm 8315、Audit 5947、Recipe 1489、Batch 1171、BatchInfo 1588；数据库累计提交 75278 行。相对 14 小时 29 分钟点新增 4808 行，约 `80.1 行/分钟`；连接保持 `connected`，`faults=2`、`errors=0`，pending/retry/dead-letter、stderr 与精确错误命中数均为 0。

`02:50:34` 第 13 条独立数据库快照显示六张表最新采集时间均位于 `02:48:20`–`02:50:32`。最近五个资源采样保持约 37986304–37998592 字节工作集、21 线程、329 句柄，未见资源抬升。

## 16 小时窗口：非计划数据库中断与自动恢复

`2026-07-31 03:00:26.609 +08:00` 起，MySQL `192.168.50.22:3306` 开始拒绝连接（错误码 2003、Windows 10061）；最后一条同类连接错误位于 `03:14:22.097`，可见异常窗口约 13 分 55 秒。本次不是测试程序安排的扰动，也未重启采集器。日志精确统计得到 552 条带 logger `ERROR` 级别的记录和 120 个 traceback 起点；`collector_stderr.log` 仍为 0 字节，异常均由采集器自己的结构化日志记录。

中断期间 OPC UA 状态始终为 `connected`，事件计数持续增长；数据库累计提交数在 `76061` 暂停，内存重试队列于 `03:12:35.642` 达到观测峰值 1001 行。数据库恢复后，`elapsed=57240.4s` 的累计提交数跃升至 `77273`，单分钟补写 1212 行；随后恢复到约 80 行/分钟的正常速率。到 `elapsed=59520.1s`，六组事件累计 80307 条、数据库累计提交 80295 行，进程仍在运行，PLC 为 `connected`，程序进度字段显示 pending/retry/dead-letter 均为 0。需要注意，测试驱动的 `errors=0` 只统计它自身安排的 OPC UA 断线恢复错误，并不包含上述数据库 logger 错误；最终结论不会仅依据该字段或 `passed` 布尔值。

`03:54:00` 的第 15 条独立数据库快照已成功连接数据库，六张表最新采集时间分别位于 `03:52:22`–`03:53:58`，证明恢复后数据库继续实时增长。采集器 PID 3424 当前有 2 条到 MySQL 的 Established TCP 连接，未发现大量遗留套接字；代码路径使用 SQLAlchemy `pool_pre_ping=True`，连接故障批次由 `data_storage.retry_queue` 保留并重放。

本次中断伴随资源平台抬升：中断前工作集约 37,998,592 字节、21 线程、329 句柄；`03:28` 工作集约 79,060,992 字节、21 线程、425 句柄，至 `03:53` 保持约 79,060,992 字节、21 线程、377 句柄。句柄已从峰值回落 48 个，但工作集尚未回落到中断前水平，继续观察到测试结束并结合最终资源曲线判断。基于当前证据，采集与重试恢复功能有效，但“全程无数据库失败”的严格稳定性条件已经不满足；是否存在数据缺口以及资源是否最终回收，等待最终数据库对账和停机指标确认。

## 17 小时 30 分钟运行检查点与 PVI 预检

`elapsed=63000.4s`：Product 64162、Alarm 9335、Audit 6691、Recipe 1678、Batch 1323、BatchInfo 1753；数据库累计提交 84929 行。连接保持 `connected`，`faults=2`、测试驱动 `errors=0`，pending/retry/dead-letter 均为 0。数据库 logger 错误仍为上述 552 条，最后错误时间仍为 `03:14:22.097`，未出现新的数据库异常；`collector_stderr.log` 为 0 字节。

`04:52:19` 第 16 条独立数据库快照显示六张表最新采集时间均位于 `04:51:07`–`04:52:19`，恢复后已连续增长约 1 小时 38 分钟。最近四个资源采样继续保持 79,060,992 字节工作集、21 线程、377 句柄；内存和句柄平台没有继续抬升，但仍未回落到数据库中断前基线。

18 小时扰动前 PVI 只读预检 8/8 成功：Recipe/Product/Alarm/Audit 四个 `bManualTrigger=false`，`uiManualCommand=0`，Alarm/Audit 索引仍为上一轮的 41/53，批次为打开状态。下一轮将使用未重复的 Alarm/Audit 索引 65/77，并在写入前再次读取批次状态决定命令。

## 18 小时运行检查点与第三次断线

`elapsed=64800.1s`：Product 65988、Alarm 9608、Audit 6866、Recipe 1722、Batch 1361、BatchInfo 1787；数据库累计提交 87311 行。`2026-07-31 05:21:25.019 +08:00` 执行第三次受控 OPC UA 传输断开，同秒 `05:21:25.090` 重建 subscription（ID 599453080），日志可见恢复耗时约 71 ms。该分钟状态已恢复为 `connected`，`faults=3`、测试驱动 `errors=0`，pending/retry/dead-letter 均为 0；随后累计提交持续增长，到 `elapsed=66660.2s` 已达 89852 行。

断线后的五个资源采样保持约 79,060,992–79,065,088 字节工作集、21 线程、377 句柄，没有因第三次 OPC UA 重连产生新的资源阶跃。数据库 logger 错误仍为 552 条且最后时间仍为 `03:14:22.097`，`collector_stderr.log` 仍为 0 字节。

## 第 5 轮：18 小时断线恢复后的人工触发

- 写前 `05:53:34` 独立数据库快照已落盘；PVI 8/8 读取成功，四个手动触发均为 `false`、`uiManualCommand=0`，批次为打开状态。
- `05:53:53`–`05:53:55`，MCP PVI 写入 7/7 成功：Alarm 索引 65、Audit 索引 77，Recipe/Product/Alarm/Audit 四个手动入口置位，并写入 `uiManualCommand=2` 结批。成功审计：`br_device_autodev/var/audit/2026-07-30/215355_696237_plc_write_pvi_succeeded_ce16f841.json`。
- 8 秒后的独立 PVI 回读 8/8 成功，四个 `bManualTrigger=false`、`uiManualCommand=0`，索引保持 65/77；批次已被持续仿真逻辑重新打开。
- 数据库快照 `05:53:34.159` → `05:54:28.053`：Recipe `+5`、Product `+53`、Alarm `+15`、Audit `+9`、Batch `+2`、BatchInfo `+3`，六组均成功新增。结构化记录已追加到 `pvi_disturbances.jsonl`；累计 5 轮、35/35 项写入成功。

## 19 小时 32 分钟运行检查点

`elapsed=70320.5s`：Product 71622、Alarm 10419、Audit 7454、Recipe 1875、Batch 1479、BatchInfo 1953；数据库累计提交 94789 行。相对 18 小时点新增 7478 行，约 `81.3 行/分钟`；连接保持 `connected`，`faults=3`（三次计划断线）、测试驱动 `errors=0`，pending/retry/dead-letter 均为 0。

`06:54:15` 第 19 条独立数据库快照显示六张表最新采集时间均位于 `06:52:37`–`06:54:17`。数据库 logger 错误仍为 552 条且最后时间仍为 `03:14:22.097`，恢复后约 3 小时 40 分钟没有新增数据库错误；`collector_stderr.log` 仍为 0 字节。最近五个资源采样完全保持约 79,065,088 字节工作集、21 线程、377 句柄，未继续增长，但中断后资源平台仍未回落。

## 20 小时 33 分钟运行检查点

`elapsed=73980.1s`：Product 75355、Alarm 10968、Audit 7828、Recipe 1967、Batch 1555、BatchInfo 2051；数据库累计提交 99700 行。相对 19 小时 32 分钟点新增 4911 行，约 `80.5 行/分钟`；连接保持 `connected`，`faults=3`、测试驱动 `errors=0`，pending/retry/dead-letter 均为 0。

`07:55:09` 第 20 条独立数据库快照显示六张表最新采集时间均位于 `07:53:40`–`07:55:08`。数据库 logger 错误总数与最后时间均无变化，`collector_stderr.log` 仍为 0 字节。`07:53` 资源采样短暂显示 23 线程，`07:55` 即时复核已回到 21 线程；句柄保持 377，工作集约 79.1 MiB，未伴随新的资源阶跃。

## 21 小时 34 分钟运行检查点

`elapsed=77640.1s`：Product 79085、Alarm 11505、Audit 8232、Recipe 2063、Batch 1633、BatchInfo 2144；数据库累计提交 104652 行。相对 20 小时 33 分钟点新增 4952 行，约 `81.2 行/分钟`；连接保持 `connected`，`faults=3`、测试驱动 `errors=0`，pending/retry/dead-letter 均为 0。

`08:55:38` 第 21 条独立数据库快照显示六张表最新采集时间均位于 `08:54:01`–`08:55:38`。数据库 logger 错误总数仍为 552，最后错误仍停留于 `03:14:22.097`，`collector_stderr.log` 为 0 字节。最近六个五分钟资源采样完全一致：79,065,088 字节工作集、21 线程、377 句柄；中断后平台稳定无继续增长。

## 22 小时 10 分钟运行检查点与最终扰动预检

`elapsed=79800.2s`：Product 81285、Alarm 11811、Audit 8465、Recipe 2122、Batch 1677、BatchInfo 2203；数据库累计提交 107539 行。相对 21 小时 34 分钟点新增 2887 行，约 `80.2 行/分钟`；连接保持 `connected`，`faults=3`、测试驱动 `errors=0`，pending/retry/dead-letter 均为 0。

`09:31:42` 第 22 条独立数据库快照显示六张表最新采集时间均位于 `09:30:13`–`09:31:41`。数据库 logger 错误总数与最后错误时间无变化，`collector_stderr.log` 为 0 字节；最近五个资源采样保持 79,065,088 字节工作集、21 线程、377 句柄。

23 小时最终扰动前 PVI 只读预检 8/8 成功：四个 `bManualTrigger=false`、`uiManualCommand=0`，Alarm/Audit 索引为上一轮的 65/77，批次为打开状态。最终一轮将使用未重复索引 91/7，并在写入前再次读取批次状态。

## 第 6 轮：约 23 小时最终人工触发

- 写前 `09:56:43` 独立数据库快照已落盘；PVI 8/8 读取成功，四个手动触发均为 `false`、`uiManualCommand=0`，批次为打开状态。
- `09:57:00`–`09:57:03`，MCP PVI 写入 7/7 成功：Alarm 索引 91、Audit 索引 7，Recipe/Product/Alarm/Audit 四个手动入口置位，并写入 `uiManualCommand=2` 结批。成功审计：`br_device_autodev/var/audit/2026-07-31/015703_163460_plc_write_pvi_succeeded_c66a428f.json`。
- 首次写后回读与后续 `10:58` 独立 PVI 复核均确认四个 `bManualTrigger=false`、`uiManualCommand=0`，索引保持 91/7；批次已由持续仿真逻辑重新打开。
- 精确查询 `09:56:50`–`09:57:30` 触发窗口：Recipe 4 行、Product 86 行、Alarm 12 行、Audit 4 行、Batch 1 行、BatchInfo 4 行，六组均在扰动窗口内写入数据库。宽窗口快照 `09:56:43` → `10:58:44` 六表增量也全部为正。
- 结构化记录已追加到 `pvi_disturbances.jsonl`；累计 6 轮、42/42 项写入成功，Alarm/Audit 的六组索引均不重复。

## 23 小时 37 分钟运行检查点

`elapsed=85020.2s`：Product 86598、Alarm 12581、Audit 9004、Recipe 2257、Batch 1787、BatchInfo 2315；数据库累计提交 114489 行。连接保持 `connected`，`faults=3`、测试驱动 `errors=0`，pending/retry/dead-letter 均为 0。数据库 logger 错误仍为 552 条，最后错误仍停留于 `03:14:22.097`，`collector_stderr.log` 为 0 字节。

数据库中断后的资源抬升最终已回收：工作集从约 79.1 MiB 于 `10:38` 前回落到约 36.0 MiB；`10:58` 为 37,867,520 字节、21 线程、377 句柄。线程维持基线，句柄较中断前仍高 48 个但已长期稳定；最终将在采集器正常退出后结合停机指标判断是否存在残留。

# ✅ 已完成：连接恢复扰动

在稳定性窗口的 6、12、18 小时点各安排一次 OPC UA 传输断开。最终报告记录三次恢复耗时分别为 0.141、0.188、0.109 秒，三次均恢复成功，恢复前后 subscription handle 均为 6；扰动只断开采集器客户端连接，没有重启或下载 PLC。

# ✅ 已完成：24 小时结果汇总与恢复确认

正式窗口于 `2026-07-31 11:21:25 +08:00` 正常结束，实际持续 `86400.141` 秒。测试程序内置判定为 `passed=true`，采集器及两个资源监控均记录正常退出。

最终事件数：

- Product 88002（time 86401、variable 1601）
- Alarm 12780、Audit 9153、Recipe 2293、Batch 1817、BatchInfo 2339
- 六组事件合计 116384；数据库处理器累计成功提交 116340 行

数据库行数基线 → 终值及增量：

- Recipe 6855 → 9297（`+2442`）
- Product 45052 → 138958（`+93906`）
- Alarm 79781 → 93414（`+13633`）
- Audit 76139 → 85906（`+9767`）
- Batch 1122 → 2057（`+935`）
- BatchInfo 7448 → 9899（`+2451`）

六轮 PVI 外部扰动累计 42/42 项写入成功，Alarm/Audit 索引均不重复。每轮手动入口和 Batch 命令均被 PLC 消费并复位，每轮数据库窗口六组增量均为正；最终一轮的 40 秒精确窗口也确认六组写入。`2026-07-31 12:00:49` 另执行明确恢复写入 5/5 成功，审计文件为 `br_device_autodev/var/audit/2026-07-31/040052_559673_plc_write_pvi_succeeded_1cc54586.json`；随后 PVI 8/8 回读确认四个 `bManualTrigger=false`、`uiManualCommand=0`，Alarm/Audit 索引保留 91/7，专用测试 PLC 批次仿真继续处于打开状态。

`12:01:06` 最终独立数据库快照成功，六张表最新采集时间位于 `11:59:50`–`12:01:05`。该快照晚于本次采集器退出，说明专用测试环境仍有既有数据源继续写入；本次测试自身的 24 小时增量以 `stability_24h.json` 的启动/停机同进程快照为准。

资源记录共 284 个运行样本，工作集范围 34,365,440–79,151,104 字节，线程范围 15–23，句柄范围 305–428。数据库中断后工作集曾升至约 75.5 MiB，但最终样本已回落到 37,916,672 字节（约 36.2 MiB）；线程为 21、句柄为 377，均无继续增长。两个资源监控均在 `11:23` 记录 PID 3424 已退出，进程退出后系统资源全部由操作系统释放。

快速回归命令 `uv run --directory _Prj/SD_SMA_DATA_COLLECTOR python -m pytest tests -q -m "not integration"` 结果为 `212 passed, 4 warnings`。

## 最终判定

- **功能与恢复能力：通过。** 连续运行时长、六组采集、数据库增量、三次 OPC UA 断线恢复、六轮 PVI 扰动、PLC 最终复位、正常停机与无队列残留均有直接证据。
- **程序内置验收：通过。** `stability_24h.json` 为 `passed=true`，结束前连接为 `connected`，测试驱动 `errors=[]`，dead-letter 与 shutdown remaining 均为 0。
- **严格“全程零数据库失败”稳定性：未通过。** `03:00:26`–`03:14:22` 出现非计划 MySQL 拒绝连接，日志有 552 条 logger ERROR 和 120 个 traceback；最终指标记录失败写入尝试 1792 行、入队并重试 1800 行。系统自动重连并清空重试队列，dead-letter=0、shutdown remaining=0，恢复后持续正常写入约 8 小时且资源最终回落，但该外部数据库中断仍属于本次稳定性窗口内的真实故障。

因此本次交付结论为：**24 小时测试执行完成，采集器的断线恢复、重试重放和业务触发链路通过；若验收标准要求测试环境全程无数据库错误，则本轮应记为“严格稳定性不通过（外部 MySQL 中断），恢复能力通过”。**
