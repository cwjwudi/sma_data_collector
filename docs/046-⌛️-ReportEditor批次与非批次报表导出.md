# ReportEditor：批次报表 vs 非批次报表导出

> 本文件为 **新需求看板**；规则见 [CLAUDE.md](../CLAUDE.md)。  
> **登记日期**：2026-08-07。  
> **拍板日期**：2026-08-08（Q1–Q6、Q7–Q9 现场确认）。  
> **范围**：报表生成落盘路径与模版/触发绑定配置。  
> **关联**：历史子文件夹 [010](010-✅-ReportEditor历史报表子文件夹穿透.md) · U 盘拷移 [022](022-✅-ReportEditor历史报表复制到U盘.md) · [027](027-✅-ReportEditor历史报表拷移与U盘审计.md)。

---

# ✅ 已完成：产品口径登记

## 区分两种报表生成

| 类型 | 与批号关系 | 落盘规则 |
|------|------------|----------|
| **批次报表** | 跟批号走（**现网默认行为**） | 在大文件夹下建以**批次号命名**的子文件夹，本批次全部报表文件放该子文件夹内 |
| **非批次报表** | **与批号无关** | 在模版上**单独指定目标文件夹**（本机绝对路径）；触发生成后，文件直接写入该指定文件夹（不再按批号建子目录） |

## 要点

- 批次报表：保持当前导出方式，不改语义。  
- 非批次报表：目标路径是**模版侧**配置项，不是全局导出根下的批号子目录。  
- 触发生成后两类路径行为必须明确可区分、可验收。

---

# ✅ 已完成：Q1–Q9 产品拍板（2026-08-08）

| # | 结论 | 口径 |
|---|------|------|
| **Q1** | **A** | 「批次 / 非批次」类型**只挂模版**；模版定了就全局按这种落盘（绑定不覆盖、不另挂类型） |
| **Q2** | **A** | 非批次「目标文件夹」**只允许本机绝对路径**（如 `D:\Reports\Daily`）；相对路径/非法路径 → **显式失败**，禁止 silently 落到批号目录 |
| **Q3** | **A** | 非批次产出**进历史报表 + 写审计**（与批次一样能看、能拷 U 盘） |
| **Q4** | **A** | 手动「导出 PDF」**也按类型走**（批次建批号目录 / 非批次去模版指定目录） |
| **Q5** | **A** | 手动导出且类型=批次、但当前无批号 → **报错，禁止导出**（不弹窗填批号、不回落全局根） |
| **Q6** | **A** | 非批次文件名规则与现网批次相同（模版名 + 时间戳等），**只是目录不同**；本期不单独配文件名模板 |
| **Q7** | **B** | 历史支持**多根**：全局导出根 + 各模版 `nonBatchOutputDir` 聚合扫描（兑现对齐 Q3「能看、能拷」） |
| **Q8** | **B** | 非批次目标目录不存在时 → **创建该目录（及父目录）**；创建失败/非目录/非绝对路径仍显式失败 |
| **Q9** | **C** | 批次批号：文件名 OPC **或** 目录 OPC **任一有值**即可建 `全局根/<批号>/`；**都没有**才失败（取消静默回落全局根） |

### 配置落点（实现用）

- **模版字段**（名称待定，语义固定）：
  1. `reportKind`：`batch` | `nonBatch`（或缺省视为 `batch`）
  2. `nonBatchOutputDir`：本机绝对路径；仅当 `reportKind=nonBatch` 时必填
- **自动结批绑定**：不增加类型字段；沿用所绑模版的类型与目标目录。
- **历史扫描**：多根 = `watchDir`/`autoExportDir` + 已配置非批次模版的绝对路径（去重）；U 盘拷移沿用现网，覆盖多根可见文件。
- **批号解析（batch）**：复用现有优先级（文件名 OPC → 目录 OPC）；任一有效 → `全局根/<批号>/`；皆无效 → 失败，不写根。
- **旧模版缺省**：无字段 → `batch`；`nonBatchOutputDir=""`。

---

# ⌛️ 未完成：模型 / UI / 导出路径落地

## 拟改方向（待实现）

1. **模版模型 + UI**：类型开关（批次报表 / 非批次报表）；非批次时露出「目标文件夹」绝对路径输入（可选手改）。  
2. **导出调度**（自动结批 + 手动导出共用）：
   - `batch` → 解析批号（Q9C）→ `全局导出根/<批号>/`；无批号 → **失败**。  
   - `nonBatch` → `模版.nonBatchOutputDir/`；非绝对/非法 → **失败**；不存在则 **mkdir recursive**（Q8B）；禁止落到批号目录。  
3. **历史报表多根（Q7B）**：聚合全局根与各非批次目录；左栏/列表可见；审计与 U 盘拷移覆盖这些路径。  
4. **文件名**：复用现网批次命名逻辑，不另开模板。  
5. **单测 / 手测**：两种类型各触发一次（自动 + 手动）；无批号批次导出须失败；非批次目录不存在时自动创建；历史多根可见；审计有记录。

## 工程默认（不必再拍板）

- 保存时：校绝对路径格式；导出时：硬校可写/可创建。  
- 手动批次批号来源：与自动结批同一套 OPC。  
- 审计不依赖批号；可在 detail 附 `reportKind` / `outputDir`。

## 验收（实现后）

- [ ] 批次报表：落到 `大文件夹/<批号>/`，本批多文件同目录；文件名 OPC 或目录 OPC 任一有效即可。  
- [ ] 批次且两路 OPC 皆无有效批号 → 禁止导出（不回落全局根）。  
- [ ] 非批次报表：落到模版指定绝对路径；目录不存在则创建后写入。  
- [ ] 两类互不串路径；相对路径/非法路径时报错可读。  
- [ ] 手动导出遵守同一套类型规则。  
- [ ] 历史分屏多根可见非批次产出，且有审计；可拷 U 盘。  
- [ ] 非批次文件名与批次同规则（仅目录不同）。  
